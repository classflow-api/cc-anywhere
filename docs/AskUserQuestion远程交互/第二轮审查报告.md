# 第二轮代码审查报告（边界值与业务影响）

## 文档信息
- 审查日期：2026-05-15
- Agent ID：全新独立子 Agent（阶段九 第二轮 Review）
- 审查重点：边界值 / 并发 / 现有业务影响 / 接口兼容性 / 配置 / 权限
- 审查范围：28 个文件（13 新增 + 15 修改），重点核对第一轮 6 个 🟡 警告及边界场景

## 审查结论
- [ ] 通过：未发现阻塞问题
- [x] 不通过：存在 2 个 🔴 阻塞问题需修复（M4 验收门槛专项），外加 7 个 🟡 警告 / 3 个 🟢 建议

---

## 发现的问题

| 序号 | 严重程度 | 文件:行号 | 关联场景/规则 | 问题描述 | 修复建议 |
|-----|---------|----------|--------------|---------|---------|
| 1 | 🔴 阻塞 | `AndroidClient/lib/features/chat/widgets/ask_user_question_card_realtime.dart:94-118` 与 `Server/internal/router/router.go:71-87` | F4-S1 / R-F4-003 / M4 上线门槛 | **M4 tool_approval Phone 端 UI 缺失 + Server 路由缺 ask.tool_approval.answer**：Phone 端 `AskUserQuestionCardRealtime` 在 `askKind == "tool_approval"` 时只把 header 文案改成"Claude 请求工具批准"，正文仍按 `questions[]` 渲染。但 Mac 端 `HookIpcServer.handleAsk:425-428` 在 tool_approval 分支不解析 questions（保持 nil），因此 Phone 端展示一个空卡片，仅有"取消"按钮，**用户无法点击允许/拒绝**。即使 Phone 端发了什么消息，server `router.RouteFromPhone` 也没有 `ask.tool_approval.answer` case → 走 default → `slog.Debug("unroutable from phone")` 静默丢弃。Mac 端 `DependencyContainer:158-162` 又订阅了 `ask.tool_approval.answer` 等待入站，**整条链路是断的**。结果：M4 ON 后 Bash/Write/Edit 调用必然等到 5min inner timeout → hook 返回 deny → Claude 拒绝工具。M4 不可用。 | 三处同步修复：① Phone 端 `_buildHeader` 之外，在 `_questionBlock` 同级增加 `tool_approval` 分支：渲染 `tool_input` 摘要 + "允许" / "拒绝" 两个按钮，按钮回调发新 type `ask.tool_approval.answer { request_id, decision, reason }`。② Server `protocol/messages.go` 新增 `TypeAskToolApprovalAnswer = "ask.tool_approval.answer"`，`router.go RouteFromPhone` 增加该 case 路由到 mac。③ Mac 端 `ProtocolMessage.swift` 新增对应 payload 解析（当前 `DependencyContainer.handleAskToolApprovalInbound` 用裸 dict 解析，建议改成强类型 struct）。M4 上线必须先完成本项。 |
| 2 | 🔴 阻塞 | `MacClient/Sources/CCAnywhere/Services/HookIpcServer.swift:444-447` | R-F5-002 / F1-S3 / 第一轮 #6 残留 | **超时回收时未清除 `hookPushedToolUseIds`，导致 fallback 路径下 phone 端漏看 AskUserQuestion 卡片**：`handleAsk` 在 ask 请求**注册时**就调 `jsonlWatcher.markHookPushed(toolUseId)`（line 444-447），TTL 10 分钟。若 5 分钟后 inner timeout 触发，hook 返回 deny → Claude SDK fallback TUI 弹原 AskUserQuestion 弹窗 → 用户在 TUI 内回答 → JSONL 落盘 `tool_use:{id:toolu_xxx}` + `tool_result`。此时 JSONLWatcher 看到 `tool_use_id` 在 hookPushedToolUseIds 集中（剩 5 分钟 TTL），**跳过这条事件不推 phone**。Phone 端因此完全看不到这次 AskUserQuestion 的历史记录（既没收到 hook 实时卡片，也没收到 JSONL 旁观补拉）。违反场景 F1-S3 期望（"phone+Mac 都不响应 timeout"后还是要让用户能在 phone 端补拉历史回看本次问答），属于已落盘消息的丢失。第一轮 #6 已识别但未修。 | 二选一：① 把 `markHookPushed` 的调用从 ask 注册时移到 `resolveAsk` 内（answered 成功时才标记，timeout/cancelled 不标记），最简洁。② 扩展 `HookIpcJsonlSink` 加 `clearHookPushed(toolUseId:)`，在 `reapExpired` 内反向清除。推荐 ①。需 progress_pre/post 仍保留 markHookPushed（它们本来就是 success/失败都对应一条 JSONL，应当 dedup）。 |
| 3 | 🟡 警告 | `MacClient/Sources/CCAnywhere/Services/HookIpcServer.swift:425-428` 与 `Views/AskQuestionCardView.swift:236-273` | §3.4.1 / 边界值"空 questions" | **空 questions 边界**：Mac 端 `handleAsk` 在 askKind == "user_question" 时调用 `parseQuestions(from:)`。若 `tool_input.questions` 字段缺失或为空数组 → 返回 `[]`（Swift 端实测：`parseQuestions` 当 `obj["questions"]` 为空数组时遍历 0 次，`result` 为 `[]`）。Mac AskQuestionCardView `UserQuestionBody` `canSubmit` 在 `qs.isEmpty` 时返回 false（line 408），按钮 disabled。但卡片整体仍展示，用户无法答题也无法取消（无取消按钮）→ 卡片"卡住"直到 5min timeout。Phone 端 `AskUserQuestionCardRealtime` 在 `qs.isEmpty` 时只渲染 header + "取消" 按钮，用户能 dismiss。**双端体验不一致 + Mac 端"卡死"卡片不友好**。 | 在 `HookIpcServer.handleAsk` 加防御：`if askKind == "user_question" && (questions == nil || questions!.isEmpty)` 直接 resolve 为 `{}` 软失败（让 SDK 走 TUI fallback），并日志 warn。这是异常上游 schema，但应优雅退化而非卡死 Mac UI。 |
| 4 | 🟡 警告 | `Server/internal/router/router.go:71-87` / `protocol/messages.go:387-389` | R-F1-014 / NFR-S3 / PRD R6 | **answers 字段 server 端无长度限制（R6 风险未对策）**：PRD §7.1 R6 明确"answers 字段 server 限长 8KB，超过返回 400"。当前 server `RouteFromPhone` 收到 `ask.question.answer` 完全透传（line 78-82），无任何长度校验。WS 帧上限是 4MiB（connection.go:31），与 8KB 之间有 511 倍的窗口。Phone 端 Other 输入限长 200 字符（NFR-C5），但 multiSelect + 多 question 拼接后可达数 KB；恶意 phone 或 bug 客户端可发巨大 answers。Mac 端 hook bridge 把 answers 直接 echo 给 Claude SDK 作 `updatedInput.answers`，超长会拖慢 SDK 解析甚至触发其内部限制。 | server 端 `RouteFromPhone` 在 `TypeAskQuestionAnswer` 分支前增加：解析 `AskQuestionAnswer.Answers`，计算总字节 / 字段总长度，超过 8KB 返回 `errorEnvelope(400, "answers too large")`。Mac 端 ipc handleAsk 接 ws answer 时同样做一次防御性截断。 |
| 5 | 🟡 警告 | `MacClient/Sources/CCAnywhere/Services/HookIpcServer.swift:436` + `HookIpcServer.swift:417-418` | R-F1-006 / 安全边界 | **tab_id 校验只匹配 UUID 格式，未防御空字符串与畸形输入的早期 fail**：`handleConnection` line 389-394 通过 `tabRouter.isActive(tabIdString:)` 校验 tab_id。`TabManagerHookIpcTabRouter.isActive`（HookIpcAdapters.swift:125-130）做了 `UUID(uuidString: tabIdString)` 解析，非 UUID 直接返回 false → 走软失败路径返回 `{}`。**但 tool_use_id 为空字符串 / 任意字符串**（line 418 `req.toolUseId ?? ""`）无校验：空字符串被 `markHookPushed("")` 跳过（line 445 `if !toolUseId.isEmpty`），但 winner 锁 register 的 PendingAskRequest 用 `toolUseId: ""` 是允许的。后续 `ask.question.pending` payload 出去的 `tool_use_id: ""` Phone 端 `tryFrom` 仍走 `toolUseId is! String || .isEmpty` 校验，但 `tryFrom` 对 toolUseId 只查 String，没有 require 非空（line 166 `data['tool_use_id'] as String? ?? ''`）。极端情况下空 tool_use_id 会导致 dedup 完全失效（一直 markHookPushed("") 无效 + JSONLWatcher.extractToolUseIds 空字符串永不命中）。 | 在 `handleAsk` 入口若 `req.toolUseId == nil || .isEmpty`，依旧走 ask 流程（不阻塞 user_question），但 progress_pre/post 应直接软失败放行（它们的 dedup 价值完全依赖 tool_use_id）。Phone 端 `ToolProgressPrePayload.tryFrom` 已在 line 287 校验空字符串返回 null，OK。Mac 端 `handleProgressPre/Post:498-535` 应仿照增加 `if toolUseId.isEmpty { return early }` 跳过 ws 推。 |
| 6 | 🟡 警告 | `AndroidClient/lib/features/chat/widgets/ask_user_question_card_realtime.dart:445-466` | R-F1-014 / 多选边界 | **Phone 端 multiSelect 多题部分作答时仍可提交**：`_submitMultiSelections` 在每个 question 未选 + Other 也为空时 `continue`，最终若 `answers.isEmpty` 才 return。多题场景（如 3 个 question，用户只答了第 1 个）会发送 `{"q1": "选项A"}`，**q2 / q3 缺失**。Mac 端 hook bridge 把这个 answers 当作完整答案给 Claude SDK 的 updatedInput，可能让 Claude 继续而忽略未答题，行为与"用户认为我答了 3 题"的预期不符。Mac 端 AskQuestionCardView `canSubmit` 要求**所有 question 都有选择**（line 409-415），与 phone 端不一致。 | Phone 端 `_submitMultiSelections` 入口校验"每个 question 至少有一个 selected 或 Other 文本"，否则 return 不提交，并提示 SnackBar"还有未答题目"。与 Mac 端 `canSubmit` 一致。 |
| 7 | 🟡 警告 | `MacClient/Sources/CCAnywhere/Services/SettingsJsonInstaller.swift:447-449` + 第一轮 #8 残留 | R-F1-009 / 安全边界 | **command 路径包含双引号时未转义**：`makeCommandString` 拼成 `"<path>" subcommand`，若用户 home 路径含 `"`（罕见但理论可能），生成的 command 字段 JSON 内被解析为 `"\""<path part 1>\"" <path part 2>"`，Claude `sh -c` 时 shell 解析失败。同样 `commandStringMatchesBridgePath` / `entryCommandSubcommand` 反向解析时也不处理转义。第一轮 #8 已列，未修。 | 在 `makeCommandString` 内 `let escaped = hookBridgePath.path.replacingOccurrences(of: "\"", with: "\\\"")`；反向解析也对应处理。低优先级，可放到后续。 |
| 8 | 🟡 警告 | `MacClient/Sources/CCAnywhere/Services/HookIpcServer.swift:615-634` + 第一轮 #10 残留 | R-F2-005 / §3.2.6 | **parseToolResponse 缺省字段语义未明确**：当 `resp == nil`（hook bridge 没发 tool_response，理论上 PostToolUse 总有）/ 非 object / 缺 `is_error` 字段时，函数返回 `(true, nil)` 视为 success，phone 端不会展示失败 toast。这与 Claude PostToolUse 真实 schema 的差异需要 echo 实验验证。 | 暂记跟踪；echo 实验后视真实 stdin 字段补一行回退到 `(true, nil)` 的注释，并对 `obj["is_error"]` 缺失但 `obj["error"]` 非空的情况兜底视为失败。 |
| 9 | 🟡 警告 | `MacClient/Sources/CCAnywhere/Resources/cc-anywhere-hook-bridge.py:190-200` + 第一轮 #5 残留 | F3-S1 / NFR-O2 | **Notification 字段映射可疑**：第一轮 #5 已识别 `cmd_notification` 从 stdin 顶层读取 `message` / `title` / `type` 但实际 Claude SDK Notification schema 是嵌套 `{ notification: { type, message } }`。当前实现 phone 端永远拿默认值 `notification_type="idle"`，颜色区分 R-F3-003 退化为单色。本轮代码与上轮同。 | echo 实验真实 Notification stdin schema 后补正字段抽取；至少应同时尝试 `stdin_json.get("notification", {}).get("type") or stdin_json.get("type")` 兼容两种 schema。 |
| 10 | 🟢 建议 | `MacClient/Sources/CCAnywhere/Services/HookIpcServer.swift:145-168` + 第一轮 #11 残留 | NFR-S1 / R-F1-005 | **socket chmod 时序 race**：异步轮询 chmod 最长 2s 窗口期内 socket 文件以默认权限存在；同 host 同用户其他进程理论可在窗口期内 connect 监听。低危。 | 改为 `socket(2) + bind(2) + chmod` 一气呵成再交给 NWListener 复用，或在 NWListener.start 前 fchmod(2)。 |
| 11 | 🟢 建议 | `AndroidClient/lib/services/dedup_service.dart:42-60` | R-F5-004 / 持久化 corrupt 容错 | **DedupService 持久化数据 corrupt 的恢复**：当前 `_ensureLoaded` 在 JSON 解析失败时 catch 后静默 `_loaded = true`，未清除 corrupt 的 SharedPreferences 值。下次启动若仍 corrupt → 每次启动都白做一次 jsonDecode → 总是失败，但功能正常（in-memory 重新跑）。 | catch 内增加 `_prefs.remove(_key)` 主动清理 corrupt 数据，下次启动跳过解析。 |
| 12 | 🟢 建议 | `Server/internal/router/router.go:42-65` | R-F1-013 / NFR-C3 | **server 收到 mac 发来的旧 phone 不认识的新 type 时无 fallback 通知**：旧 phone APK 已部署但 server 已升级支持新 type，旧 phone 收到未知 type 默认 inbound 路径透传（protocol_message.dart:22-34），但旧 APK 完全不订阅 ask.question.pending → 卡片不弹，用户无感知。NFR-C3 要求"不崩溃"，OK；但提示语缺失。 | server 端发现 phone 是旧版本时可 downgrade（需要客户端版本号信令），属本需求 OOS。 |

---

## 边界值检查矩阵

| 边界场景 | 检查位置 | 处理方式 | 评估 |
|---------|---------|---------|------|
| 空 questions（`[]`） | `HookIpcServer.parseQuestions:559-588` / `AskQuestionCardView.canSubmit:408` / phone `qs.length` 循环 | Mac 解析返回 `[]`，canSubmit=false，按钮 disabled，但卡片不退；Phone 渲染空 block + 取消按钮可用 | ⚠️ 见 #3（双端不一致 + Mac 卡死） |
| 单题 vs 多题 | `AskQuestionCardView.questionBlock` / phone `_questionBlock` | 两端 ForEach 渲染，验证通 | ✅ |
| 超长 question（>1000 字符） | UI 端 `Text(question)`（默认无截断）；wire 上 Mac→Phone 不截 questions（line 466-473 仅 tool_approval 截 tool_input） | UI 端 SwiftUI 自动换行；Flutter Text 自动换行；不影响逻辑 | ✅ |
| 超长 option label（>200 字符） | UI 同上 | UI 显示完整 label；wire 上不截，因 AskUserQuestion 工具原生 option label 一般短小 | 🟢 可优化 UI 截断 |
| 特殊字符（`"`, `\`, `\n`, 表情符等） | JSON encoding 透传 `JSONEncoder` / `json.dumps` / Dart `jsonEncode` | UTF-8 + Codable / std JSON 库逐层包，转义自动 | ✅ |
| tool_use_id 为空字符串 | `markHookPushed`:line 445 守门空字符串；`JSONLWatcher.extractToolUseIds` 也只 append 非 nil 字符串 | dedup 失效（空字符串永不命中），但不崩 | ⚠️ 见 #5 |
| tab_id 为空字符串 / 非 UUID | `TabManagerHookIpcTabRouter.isActive:125-130` UUID parse 失败返回 false → handleConnection 软失败 `{}` | 软失败放行 | ✅ |
| hook bridge stdin 为空字符串 | `cc-anywhere-hook-bridge.py:220-223` `json.loads("") if data.strip() else {}` → 返回空 dict 走 cmd 处理 | 退化为正常子命令处理（无 tool_use_id 等字段） | ✅ |
| hook bridge stdin 非 JSON | `cc-anywhere-hook-bridge.py:223-227` exception → echo `{}` 退出 | ✅ safe_exit_with_empty 兜底 | ✅ |
| settings.json 文件不存在 | `SettingsJsonInstaller.readSettingsObjectCreatingIfMissing:334-349` 创建父目录返回 `[:]` | 视为空对象继续 mutate 流程 | ✅ |
| settings.json 空文件 | `readSettingsObject:320` `if data.isEmpty { return [:] }` | 空文件等价空对象 | ✅ |
| settings.json 含尾随逗号等非标准 JSON | `JSONSerialization.jsonObject` throw → `InstallError.settingsCorrupted` | UI 端 HookPane:228-231 显示 lastError；不静默写入；用户需手动修 | ✅ |
| multiSelect=true 未选任何项 | Mac canSubmit:409-415 要求每题非空；Phone `_submitMultiSelections:464` 仅 answers.isEmpty 才 return | ⚠️ 见 #6（多题部分作答可漏题提交） | ⚠️ |
| pendingRequests 空 → reapExpired | `HookIpcServer.reapExpired:304-307` 先 filter，empty 直接 return | ✅ |
| Phone DedupService 持久化 corrupt | `_ensureLoaded:55-58` catch 后 `_loaded=true` 跳过 | ✅ in-memory 兜底，🟢 建议主动清理 #11 |
| answers 超长（无 8KB 限制） | server / mac 端均无限长 | ⚠️ 见 #4（R6 风险对策未实施） |

---

## 并发场景检查矩阵

| 并发场景 | 检查位置 | 安全保障 | 评估 |
|---------|---------|---------|------|
| 同一 tab 两条 AskUserQuestion 并发 | `pendingRequests[String: PendingAskRequest]` 以 requestId 为 key，actor 串行 | 两条 request_id 各自独立 continuation，互不干扰 | ✅ |
| 多 phone 同时回答 winner 锁 | `resolveAsk:263-300` actor 内 `req.answered` 检查 + mark + resume + removeValue | actor 串行化保证；首入者 win，后入者 `if req.answered` 早返 | ✅ |
| Mac App 退出时 HookIpcServer.stop 与 pending 处理 | `stop():193-198` 遍历 pending mark answered + resume `.failure(error: "server stopped")` | ✅ 退出前清理；continuation 不泄漏 | ✅ |
| settings.json 写入原子性 | `atomicWrite:397-427` tmp 写入 → `FileManager.replaceItemAt` | macOS APFS rename 原子 | ✅ |
| 多 tab 各自 ws 推送 | `WSClientHookIpcSink` 所有方法 @MainActor，HookIpcServer 通过 `MainActor.run` 桥接 | 主线程串行 + ws 内 send queue | ✅ |
| reapTimer 与 resolveAsk 并发 | actor 串行化天然保证 | ✅ 第一轮 #7 已确认 | ✅ |
| `hookPushedToolUseIds` 多线程 | `JSONLWatcher.markHookPushed` 内 `queue.async`；`isHookPushed` 闭包注入约定同 queue 调用 | DispatchQueue 串行 | ✅ |
| HookIpcServer 初始化 vs Tab 创建 | `TabManagerHookIpcTabRouter.start:104-117` 立即 `replace(with: initial)` 一次性 sync 当前 tabs，再订阅；不会漏 | ✅ |
| ChatRepository hook dedup 异步 fire-and-forget | `_checkHookDedup` 返回 future 仅记日志；实际 dedup 在 `_mergeMessages` 的 `_dedup.markSync(id)` 中 | ⚠️ JSONL 落盘极快时可能漏一次 dedup，但 hook 通道渲染本来就要展示，无双显示风险 | 🟢 第一轮 #12 |
| ProcessHost 改动并发影响 | 仅在 `makeEnvironment:163` 同步 set env，子进程 fork 后 env 一次性传递 | ✅ |

---

## 现有业务影响清单

| 改动点 | 受影响既有功能 | 影响评估 | 对策 |
|-------|-------------|---------|------|
| `JSONLWatcher.shouldEmit:304-324` 增加 hookPushed 检查 | 既有 uuid/fallback 去重逻辑 | hookPushed 在 uuid 检查**之前**，未命中时正常走原路径 → 既有去重不变；hookPushed 命中时跳过整条记录，但仅当用户已经走 hook 实时通道渲染过 → 等价行为 | ✅ 无负面影响；潜在 #2 阻塞影响 fallback 路径 |
| `ProcessHost.makeEnvironment:146-165` 增加 `CC_ANYWHERE_TAB_ID` env | 子进程 env / claude 子进程其他行为 | 仅追加一个 env key，不覆盖既有 env；claude 子进程不读未知 env 不受影响 | ✅ |
| `WSClient` 增加新 type 路由 | 既有 `msg.stream / tab.changed / input.text` 等 type 路由 | `handleIncoming:228-254` 用 switch 显式列既有 + default 路径 inbound.send；新 type 走 default → DependencyContainer 订阅 inbound 处理；既有 case 不变 | ✅ |
| `Server router.go` 增加 5 个 mac→phone 与 1 个 phone→mac case | 既有 `BroadcastToPhones` / `MacConnSend` 调用 | switch 顺序追加，既有 case 不变；无 fall-through | ✅ |
| `chat_repository._onInbound:390-405` hook dedup | 既有消息流路径 | fire-and-forget dedup 仅打日志，不影响主路径；真实 dedup 在 `_mergeMessages.markSync/hasHandledSync` 内的 toolUse 路径，分支以 `m.kind == toolUse \|\| askUserQuestion` 守门 | ✅ 第一轮 #12 已评估 |
| `DependencyContainer.wireUp:128-163` 新增 HookIpc wiring | 既有 init 顺序 | wireUp 内 init 顺序：`hookIpcTabRouter.start → setWsClient/setJsonlWatcher/setCardController → askCardController.hookIpcServer = hookIpcServer → wsClient.inbound 订阅` 全部在主线程同步，Task 包装的 setter 调用在 actor 启动前完成，无 race | ✅ |
| AppDelegate `bootstrapHookBridge:104-112` 启动时部署 | 既有 `applicationDidFinishLaunching` 流程 | deployIfNeeded 失败仅日志，不抛错；不影响后续 ws 连接 / tab 恢复 | ✅ |
| AppDelegate `applicationWillTerminate` 加 `hookIpcServer.stop` | 既有 stopAll | `Task.detached` best-effort，不阻塞退出 | ✅（best-effort 可能 socket 文件残留，但下次启动 `start():108-116` 主动 unlink） |
| `PreferencesWindow.environmentObject(container.hookPreferences)` | 既有 PreferencesRootView | environment 注入只增不减 | ✅ |
| `CCAnywhereApp.environmentObject(container.askCardController)` | MainWindow | MainWindow `@EnvironmentObject` 新增 askCardController，AppDelegate `openPreferences` 调用时 PreferencesWindow 不注入此对象（不需要） | ✅ |
| `tabs.json` 与 tab UUID | 既有 Tab 持久化 | tab UUID 不变；env 传递新增 key 不影响 tab id | ✅ |
| settings.json 写入 | 既有用户其他 plugin hooks | append 模式 + 精准前缀匹配卸载（R-F1-011），跨 plugin 共存可工作 | ✅ |
| Phone 协议 type 增加 | 旧 phone APK | 旧 APK 收到未知 type 走 `ProtocolMessage.tryFromJson:22-34` 解析包未崩溃，但不订阅新 type 不响应 | ✅ NFR-C3 满足 |

---

## 接口兼容性（破坏性变更）检查

| 检查项 | 评估 |
|-------|------|
| 6 个新 ws type 添加，旧 phone 收到未知 type | `protocol_message.dart:22-34` tryFromJson 仅解析 envelope，未知 type 不崩溃，inbound stream 静默丢；NFR-C3 ✅ |
| Server protocol struct 新增字段 | `Envelope.Data` 是 `json.RawMessage`，server 不解析内层 → 完全透传；payload struct 仅当 server 内部需要解析时才匹配字段（image.fetched 是唯一一处），新增的 6 个 type 都是透传 ✅ |
| 已有 phone 升级到新 phone 本地存储格式兼容 | `SharedPreferences` key `cc_anywhere_handled_tool_use_ids` 是新增 key，不与既有 key 冲突；`image_ref_store` 等既有 store 不变 ✅ |
| AnyJSON 在 Dart 端 jsonDecode 后类型 ↔ Swift Codable | `AnyJSON.encode/decode` 用 `JSONSerialization` 透明转 Map<String, Any> / Map<String, dynamic>，互通 ✅ |
| multiSelect 字段名 camelCase | Mac CodingKeys 保留 camelCase；Phone 读 camelCase；server 透传 ✅（第一轮 #1 已修） |

---

## M4 上线门槛专项检查

### Phone tool_approval UI 缺失评估

阅读代码确认：`AndroidClient/lib/features/chat/widgets/ask_user_question_card_realtime.dart` 全文搜 `tool_approval` 只有一处（line 121-124）：

```dart
final isToolApproval = widget.payload.askKind == 'tool_approval';
final title = answered != null
    ? '已被 ${answered.answeredBy.isEmpty ? "其他端" : answered.answeredBy} 回答'
    : (isToolApproval ? 'Claude 请求工具批准' : 'Claude 提问');
```

仅在 header 文案区分。**无 allow/deny 按钮，无 tool_input 摘要展示，无 ask.tool_approval.answer 发送逻辑**。

Mac 端 `HookIpcServer.handleAsk:425-428`：

```swift
if askKind == "user_question", let toolInput = req.toolInput {
    questions = parseQuestions(from: toolInput)
}
```

tool_approval 分支不解析 questions，所以 payload 里 questions 为 nil。Phone 端 `AskQuestionPendingPayload.tryFrom:172-176` 把 nil questions 转成空 List。Phone UI 渲染：只有 header 文案 + "取消" 按钮，**用户无法执行 allow/deny 决策**。

### ask.tool_approval.answer 路由检查

- **Mac 端**：`DependencyContainer.wireUp:158-162` 已订阅 `ask.tool_approval.answer`，等待 phone 端发包。
- **Server 端**：`router.RouteFromPhone:71-87` switch 只列了 `TypeAskQuestionAnswer`（即 `ask.question.answer`），**没有 `ask.tool_approval.answer` 这个 type 常量和 case**。phone 即便发了，会走 default → `slog.Debug("unroutable from phone")` 静默丢弃。
- **Phone 端**：无任何 `ask.tool_approval.answer` 字符串匹配。

**结论：M4 远程批准功能完全不可用**。Bash/Write/Edit 调用走 ask 通道后，phone 端无法回答 → 5min 超时 → hook deny → Claude 拒绝工具。

### M4 上线门槛阻塞

第一轮 #9 复审残留中标注"M4 上线门槛前必须解 #9"，本轮升级为 🔴 阻塞 #1，理由：
- M1-M3 default OFF，由用户主动开启。M4 default OFF，依赖主开关。M4 上线门槛是"M4 ON 后 Bash/Write/Edit phone 端能批准"。
- 当前代码状态：M4 ON → 设置写盘正确（PreToolUse Bash|Write|Edit → ask 子命令），hook bridge 正确 ask socket，Mac App 正确 ws 推 ask.question.pending → Phone 收到但 UI 卡住 → 5min 超时 → Claude 拒工具。
- 这不是 P2 优化项，而是 M4 整个功能不能上线的硬阻塞。

修复涉及：
1. Server 增 type 常量与 router case（10 行）
2. Mac 端协议 struct 与 inbound 解析（已有占位代码，可强类型化，~30 行）
3. Phone 端 tool_approval 分支 UI + 发送逻辑（~80 行）
工作量预估 0.5-1 人日。

---

## 总结

### 2 个 🔴 阻塞

1. **#1 M4 tool_approval Phone UI + Server 路由缺失**：M4 整条链路断，远程批准不可用。M4 上线前必须修。
2. **#2 超时回收未清 hookPushedToolUseIds**：fallback 路径下 phone 端漏看本次 AskUserQuestion 历史，影响 F1-S3 场景的"用户回 Mac 后还能在 phone 看到记录"体验。第一轮 #6 残留升级。

### 7 个 🟡 警告

涵盖：空 questions 双端不一致（#3，Mac 端会卡死）、answers 无 8KB 限制（#4，R6 风险对策未实施）、tool_use_id 空字符串边界（#5）、phone multiSelect 多题部分作答（#6）、command 路径双引号转义（#7，第一轮 #8 残留）、parseToolResponse 缺省字段语义（#8，第一轮 #10 残留）、Notification 字段映射可疑（#9，第一轮 #5 残留）。

### 3 个 🟢 建议

socket chmod 时序（#10，第一轮 #11 残留）、DedupService corrupt 清理（#11）、旧 phone 协议降级（#12）。

### 关于第一轮残留 🟡 警告的处理状态

| 第一轮序号 | 当前状态 | 本轮处理 |
|----------|---------|---------|
| #5 Notification 字段映射 | 未修 | 本轮 #9 跟踪，echo 实验后补 |
| #6 超时未清 hookPushedToolUseIds | 未修 | **本轮升级 🔴 #2**，影响 fallback 历史可见性 |
| #7 winner 锁注释 | 仅注释，本质 OK | 本轮无补充 |
| #8 command 路径双引号 | 未修 | 本轮 #7 跟踪，低优先级 |
| #9 缺 ask.tool_approval.answer 路由 + phone UI | 未修 | **本轮升级 🔴 #1**，M4 上线硬阻塞 |
| #10 parseToolResponse 缺省字段 | 未修 | 本轮 #8 跟踪，echo 实验时补 |

### 整体结论

- M1-M3 上线（实时 AskUserQuestion / progress / notification）：核心链路通，边界值处理可接受，存在 #2 fallback 历史漏看的非主流程问题。
- M4 上线（远程工具批准）：**完全不可用**，#1 阻塞必须修。
- R6 风险对策（answers 限长 8KB）未实施：#4 警告，建议补上。
- 第一轮 6 个 🟡 中两个升级为 🔴（#2、#1），其余沿用 🟡 / 🟢 跟踪。

**建议处理顺序：先修 #1（M4 解锁）→ 再修 #2（fallback 一致性）→ 然后 #3 / #4 / #6（用户体验）→ 最后 #7-#11 跟踪改进**。修复完成后进入第三轮 Review（代码质量与生产就绪）。

---

## 复审结果

### 复审信息

- 复审日期：2026-05-15
- 复审 Agent ID：全新独立子 Agent（第二轮 Review 复审）
- 复审范围：2 个 🔴 阻塞修复（#1 M4 三端协同 + #2 markHookPushed 时机）

### 2 个 🔴 阻塞修复状态

| # | 问题 | 修复状态 | 验证证据 |
|---|------|---------|---------|
| 1 | M4 tool_approval 三端协同缺失 | ✅ 已修复 | Server: `Server/internal/protocol/messages.go:76`（`TypeAskToolApprovalAnswer` 常量）+ `:396-400`（`AskToolApprovalAnswer` struct）+ `Server/internal/router/router.go:79`（`RouteFromPhone` 新增 case）。Mac: `MacClient/Sources/CCAnywhere/Models/ProtocolMessage.swift:289-307`（`AskToolApprovalAnswerPayload` 强类型 struct）+ `MacClient/Sources/CCAnywhere/App/DependencyContainer.swift:157-162`（inbound 订阅）+ `:187-207`（`handleAskToolApprovalInbound` 解析后调 `server.receiveApprovalFromWs`）。Phone: `AndroidClient/lib/models/protocol_message.dart:88`（`askToolApprovalAnswer` 常量）+ `:223-241`（`AskToolApprovalAnswerPayload` 类）+ `AndroidClient/lib/services/ask_question_controller.dart:174-202`（`submitApproval` 方法）+ `AndroidClient/lib/features/chat/widgets/ask_user_question_card_realtime.dart:103-104`（`isToolApproval` 切到 `_buildToolApprovalBody`）+ `:156-173`（红色"工具批准"徽章）+ `:512-572`（tool_input 摘要 + 允许/拒绝双按钮）+ `:574-580`（`_submitApproval` 触发 `onApprovalDecision`）+ `AndroidClient/lib/features/chat/chat_screen.dart:148-156`（`onApprovalDecision` 注入 → 调 `controller.submitApproval(requestId, decision)`）。 |
| 2 | markHookPushed 时机错误 | ✅ 已修复 | `MacClient/Sources/CCAnywhere/Services/HookIpcServer.swift:452-458`（`handleAsk` 注册路径已移除 `markHookPushed`，仅留代码注释说明原因）；`:278-284`（`resolveAsk` 在 winner answered 成功路径中调 `jsonlWatcher?.markHookPushed(toolUseId:)`，并 guard 空字符串）；`:312-343`（`reapExpired` 内 `req.continuation.resume(returning: .failure(error: "timeout"))` 后**不**调 `markHookPushed`，timeout 路径 JSONL 仍可推送 phone）。`MacClient/Sources/CCAnywhere/Services/HookIpcServer.swift:511, 531` 中 `handleProgressPre/Post` 各自的 `markHookPushed` 调用保留（与本次修复无关，原本就是 success/失败均推 JSONL 的 dedup 防双显）。 |

### 端到端 M4 链路自查

| # | 节点 | 关键位置 | 状态 |
|---|------|---------|------|
| 1 | M4 开关 ON → settings.json 切 ask | `SettingsJsonInstaller.swift:157-186`（`installM4` 将 `Bash\|Write\|Edit` 子命令改为 `ask`，timeout 提到 1800） | ✅ 通 |
| 2 | Claude SDK fork hook bridge | settings.json 中 PreToolUse 命中 matcher 后由 Claude SDK 内部 fork（外部协议） | ✅ 通 |
| 3 | hook bridge `cmd_ask` 发 socket | `cc-anywhere-hook-bridge.py:124-138`（构造 `kind: ask` payload，**未带 ask_kind**，由 Mac 判定）+ `:139`（`socket_call(payload, ASK_RESPONSE_TIMEOUT)` 阻塞等待） | ✅ 通 |
| 4 | HookIpcServer 收 ask 入 winner 锁 | `HookIpcServer.swift:424-499`（`handleAsk` 根据 `toolName == "AskUserQuestion"` 判定 `askKind`，非 AskUser 走 `tool_approval`；注册 `pendingRequests`；**不在此处 markHookPushed**）| ✅ 通 |
| 5 | Mac App ws 推 ask.question.pending | `HookIpcServer.swift:474-498`（携带 `askKind`、`toolName`、截断 500 字符的 `toolInput`） | ✅ 通 |
| 6 | Phone 渲染 tool_approval 卡片 | `ask_user_question_card_realtime.dart:103-104, 156-173, 512-572`（红色徽章 + tool_input 摘要 + 允许/拒绝按钮） | ✅ 通 |
| 7 | Phone 发 ask.tool_approval.answer | `ask_question_controller.dart:174-202`（`submitApproval` 通过 `_ws.send` 发 envelope，type 用 `ProtocolType.askToolApprovalAnswer`） | ✅ 通 |
| 8 | Server 路由 phone→mac | `router.go:71-87`（`RouteFromPhone` switch case 已含 `TypeAskToolApprovalAnswer`，`r.mac.MacConnSend(env)`） | ✅ 通 |
| 9 | Mac 端 ws 收到 → resolveAsk | `WSClient.swift:228-254`（default 分支 `inbound.send(msg)`） + `DependencyContainer.swift:157-162, 187-207`（filter 后 `handleAskToolApprovalInbound` → `server.receiveApprovalFromWs(requestId, decision, reason, "phone")`） | ✅ 通 |
| 10 | resolveAsk 回写 socket | `HookIpcServer.swift:221-231`（`receiveApprovalFromWs` 调 `resolveAsk(.toolApproval(decision, reason))`）+ `:259-308`（winner 锁 + `markHookPushed`）+ `:501-503`（`sendResponseAsk` 写回 hook bridge 一行 JSON `{"ask_kind":"tool_approval","decision":"...","reason":"..."}`，CodingKeys 见 `HookProtocol.swift:112-115`） | ✅ 通 |
| 11 | hook bridge 翻译 permissionDecision | `cc-anywhere-hook-bridge.py:143-149`（error 短路前置 → 早期 return `{}`）+ `:150-159`（`response.get("ask_kind") == "tool_approval"` 后 emit `permissionDecision: decision`），逻辑顺序正确，**不会被 error 短路误吞**。 | ✅ 通 |

### 复审结论

- [x] **通过：2 个阻塞全部修复，无新增阻塞**

### 残留问题（不影响本轮 🔴 阻塞修复结论）

第二轮报告中其余 🟡 / 🟢 问题（#3 - #12）按原约定推迟到第三轮 Review（代码质量与生产就绪）处理，复审未发现这些问题恶化或新增同等阻塞。下列两点作为提示，不阻塞 M4 上线：

1. `DependencyContainer.handleAskToolApprovalInbound`（DependencyContainer.swift:187-207）当前仍以裸 `data.asObject` 解析而非用强类型 `AskToolApprovalAnswerPayload.tryFrom`（虽 strut 已新增）。语义等价，但与 `handleAskAnswerInbound` 的强类型解析风格不一致；属代码风格统一项，建议第三轮 Review 时一起改造为 `decode(data, AskToolApprovalAnswerPayload.self)`。
2. `ask_question_controller.dart:submitApproval` 当前未把 `reason` 字段透传给 UI 触发回调（widget 的 `onApprovalDecision` 仅传 `decision` 字符串），即 R-F4-005 "用户附加拒绝原因"还没有 UI 收集入口；这是 PRD/规格的 P2 功能缺口而非阻塞，可在第三轮或下一次迭代中扩展。

### 复审总结

第二轮 Review 标识的 2 个 🔴 阻塞均已落实代码修复，三端关键路径（Server type+router、Mac payload struct+inbound 路由、Phone 协议常量+payload+controller+widget+chat_screen 注入）全部到位；M4 端到端 11 节点链路自查通畅，hook bridge 的 error 短路顺序正确，不会误吞 tool_approval 决策。`markHookPushed` 时机已从 ask 注册迁移到 `resolveAsk` 成功路径，`reapExpired` 不再调，satisfies F1-S3 场景下 phone 端补拉 JSONL 历史可见的要求。复审通过，可以进入第三轮 Review（代码质量与生产就绪）。
