---
review轮次: 第一轮-代码正确性与逻辑正确性
适用skill: yoolines-dev-workflow
状态: 通过
总数: {红:0, 黄:2, 绿:1}
已修复: {红:0, 黄:2, 绿:0}
升级为红: {黄:0}
入技术债: {黄:0}
未处置: {红:0, 黄:0}
重跑次数: 1
---

# TodoWrite 任务面板 - 第一轮独立代码审查报告（复审第 1 次）

> 审查员：独立 Agent（第一轮 - 代码正确性与逻辑正确性，复审第 1 次）
> 审查范围：本次 `git diff HEAD -- AndroidClient/lib/` 全部改动
> 基准文档：`docs/TodoWrite任务面板/变更说明.md`（R-T1-001 ~ R-T1-011）
> 上一轮：初审 0🔴 + 2🟡 + 1🟢，状态不通过
> 本轮：独立验证主 Agent 对 2 个 🟡 的处置 A（修复），并重跑 19 项清单

## 改动概览

| 文件 | 改动类型 | 行数（diff） |
|---|---|---|
| `AndroidClient/lib/models/todo_item.dart` | 新建 | 66 行 |
| `AndroidClient/lib/data/chat_repository.dart` | 修改 | +43 行（import / TabChatState.todos / 字段+copyWith / `_detectTodoWrite` 含 🟡-1 safeguard / `_setTodos` / `_preprocessSubAgent` 内"raw 继续进 passthrough"分支 + 注释） |
| `AndroidClient/lib/features/chat/widgets/todo_panel.dart` | 新建 | 173 行 |
| `AndroidClient/lib/features/chat/widgets/message_card_list.dart` | 修改 | +7 行（MessageKind.toolUse 分支增加 `toolName=='TodoWrite' → SizedBox.shrink()` 消音判断 + 注释） |
| `AndroidClient/lib/features/chat/chat_screen.dart` | 修改 | +5 行（import + TodoPanel 嵌入位置 _Header 与 Expanded 之间） |

> 与初审相比新增 `message_card_list.dart` 改动 — 处置 🟡-2 时把消音逻辑下沉到渲染层（与 Task tool_use 处理模式对齐：raw 进 passthrough，渲染层各自决定显示/隐藏），ChatRepository 不再"吞 raw"。

---

## 🟡 修复验证

### 🟡-1 `_detectTodoWrite` 解析失败语义 → 已修复 ✅

**修复后实现**（`chat_repository.dart:761-778`）：
```dart
List<TodoItem>? _detectTodoWrite(Map<String, dynamic> raw) {
  try {
    final inner = raw['message'];
    if (inner is! Map) return null;
    final content = inner['content'];
    if (content is! List) return null;
    for (final item in content) {
      if (item is! Map) continue;
      if (item['type'] != 'tool_use' || item['name'] != 'TodoWrite') continue;
      final input = item['input'];
      if (input is! Map) continue;
      final todosField = input['todos'];
      if (todosField is! List) return null;  // 字段缺失/非 List → safe degradation
      return TodoItem.parseList(todosField);
    }
  } catch (_) {/* 解析失败静默忽略 */}
  return null;
}
```

**独立验证**：
- `todosField is! List` 时 `return null`（line 773）— caller `_preprocessSubAgent` 内 `if (todoUpdate != null)` 不会进入 _setTodos，panel 保留旧状态 ✓
- 与初审建议的 patch 形态完全一致
- 注释明确（line 757-760）解释了"为什么这么做" — 满足项目编码规范"why over what"
- 与变更说明 §6 risk table 的 safe degradation 语义对齐

**结论**：🟡-1 处置 A（修复）有效。

### 🟡-2 整条 raw 在命中 TodoWrite 时被吞 → 已修复 ✅

**修复后实现**：

`chat_repository.dart:614-624`：
```dart
// 非 sidechain：探测 TodoWrite,把 todos 吸入主 panel。
// 第一轮 review 🟡-2：原实现在命中后 continue 把整条 raw 丢弃,若同条
// message.content 含 text + TodoWrite 会丢 text。改为 raw 仍进 passthrough,
// 消音逻辑下沉到 message_card_list 渲染层（R-T1-006 在渲染层执行）。
final todoUpdate = _detectTodoWrite(raw);
if (todoUpdate != null) {
  _setTodos(tabId, todoUpdate);
}
// （注：无 continue，raw 继续往下走 Task 检测 + passthrough.add(raw)）
```

`message_card_list.dart:292-300`：
```dart
case MessageKind.toolUse:
  // R-T1-006:TodoWrite 工具的 ToolUseCard 消音(主 session 的 todos 已经
  // 进入 TodoPanel 顶部 panel 显示,聊天流再渲染会变成重复/噪音)。
  // 子 agent 内的 TodoWrite 走 SubAgentFoldedBlock 渲染,不会到这里。
  if (m.toolName == 'TodoWrite') {
    return const SizedBox.shrink();
  }
  return ToolUseCard(...);
```

**独立验证（端到端推演）**：

场景 A — 子 agent 内 TodoWrite：
- `_preprocessSubAgent` line 585-612 sidechain 分支早已 `continue`
- 永远到不了 line 614 的 `_detectTodoWrite` 调用 ✓
- 仍走 SubAgentFoldedBlock 内 toolUse 渲染（R-T1-007 满足）✓

场景 B — 主 session 单独 TodoWrite tool_use：
- `_detectTodoWrite` 命中 → `_setTodos(tabId, todos)` → TodoPanel 更新 ✓
- raw 继续进 passthrough → `Message.fromRaw` line 152-163 按 content array index 拆分出 1 条 toolUse Message（toolName='TodoWrite'）
- `message_card_list.dart:296` `m.toolName == 'TodoWrite'` → `SizedBox.shrink()` 消音 ✓
- R-T1-006 仍满足 ✓

场景 C（🟡-2 关注的边界）— 主 session content 同时含 text + TodoWrite：
- `_detectTodoWrite` 命中 → _setTodos 更新 panel ✓
- raw 继续进 passthrough → `Message.fromRaw` 按 content array 索引拆分出 2 条 Message：
  1. text Message → `MessageKind.text` → AssistantTextCard 正常渲染 ✓（**text 不再丢失**）
  2. toolUse Message (toolName='TodoWrite') → SizedBox.shrink ✓
- 双重目标达成：text 保留 + TodoWrite 消音 ✓

**与 Task 分支模式一致性**：
- Task 分支：raw 命中 detection → 仅当 absorbedByBlock=true（即 tool_result 被折叠块吸收）时跳过 passthrough；tool_use 情况下 absorbedByBlock=false，raw 仍进 passthrough（line 676-680）
- TodoWrite 分支：始终进 passthrough，消音在 message_card_list 渲染层
- 两个分支都符合"上游不吞 raw、下游决定渲染"的设计原则 ✓

**回归风险检查**：
- `_detectTaskInRecord(raw)` 在 TodoWrite raw 上是否误命中？分析：`_detectTaskInRecord` 仅在 `type=='tool_use' && name=='Task'` 或 `type=='tool_result'` 命中。TodoWrite tool_use 的 `name=='TodoWrite'`，**不**命中 Task tool_use 分支。同条 raw 若不包含 Task / tool_result item，则 detection 返回 null → 与本次改动不互斥 ✓
- 同条 raw 含 TodoWrite + 其他普通工具（如 Bash）的 tool_use：text/Bash tool_use 仍正常渲染，TodoWrite 仍消音 ✓
- 历史回放（msg.history.response）：`_preprocessSubAgent` 同样路径调用，行为完全一致 ✓

**结论**：🟡-2 处置 A（修复）有效，且语义与 Task 分支对齐，可维护性更好。

---

## 🟢 处置说明

### 🟢-1 `chat_repository.dart:752-760` 注释 doc-comment 归属混乱 → 未处置（保留）

**当前实现**：
```dart
  /// 解析一条非 sidechain JSONL record，查 message.content 中是否含 Task
  /// tool_use 或 tool_result，提取信息。返回 null 表示与 Task 无关。
  /// R-T1-001 ~ R-T1-011：识别 TodoWrite tool_use 的 input.todos 数组。
  /// 命中返回解析后的 todos 列表（caller 用 _setTodos 覆盖当前 Tab 的 panel 状态）；
  /// 未命中返回 null。
  ///
  /// 第一轮 review 🟡-1：input.todos 字段缺失 / 非 List 时返回 null（**不**更新 panel），
  /// 否则 TodoItem.parseList 会返回 [] 把已有的 panel 清空，与 §6 safe degradation 不符。
  /// 仅当 todos 字段存在且能成功解析出 ≥0 条有效项才更新 panel。
  List<TodoItem>? _detectTodoWrite(Map<String, dynamic> raw) {
```

doc-comment 块仍同时声称描述「Task tool_use/tool_result 检测」（前两行）和「TodoWrite 检测」（后续）。Dart 工具会把整段 doc-comment 关联到 `_detectTodoWrite`（紧邻其声明 line 761），`_detectTaskInRecord` (line 786) 实际上失去了 doc-comment。

**判级**：🟢 偏好类问题，不阻塞通过。可在下一轮 polish 或视为已知遗留。

**处置**：未处置（保留为 🟢，绿色项不影响通过）。

---

## 19 项清单重跑核对

### A. TodoItem 数据模型

#### 1. ✅ `TodoItem.tryFrom` 字段校验
- `content`：`is! String || isEmpty` 双重校验（`todo_item.dart:42-43`）
- `status`：经 `TodoStatus.tryParse`，未知 → null
- `id`：optional 转换

#### 2. ✅ `TodoStatus.tryParse` 三态映射
- `'pending'/'in_progress'/'completed'` → 对应 enum；其它 → null

#### 3. ✅ `TodoItem.parseList` 类型容错
- `todosField is! List` → `const []`（注：在 `_detectTodoWrite` 的上层已加 safeguard，此处仍保留是合理冗余）
- item 非 Map / tryFrom 返回 null → 跳过

### B. ChatRepository 改造

#### 4. ✅ `TabChatState.todos` 字段
- 默认 `const []`（`chat_repository.dart:39`）
- `copyWith` 使用 `??` 保留旧值（line 57）

#### 5. ✅ `_detectTodoWrite` 仅识别 `tool_use && name="TodoWrite"` + safeguard
- 仅当 `item['type'] == 'tool_use' && item['name'] == 'TodoWrite'` 命中（line 769）
- 整体 try-catch 包裹（line 762, 776）
- **🟡-1 safeguard 已加**：`todosField is! List` → return null（line 773）保护 panel 不被无故清空

#### 6. ✅ `_setTodos` 触发 stream emit
- `_update(tabId, cur.copyWith(todos: todos))`（line 783）
- 链路：`_setTodos` → `_update` → `_state[tabId] = next` + `_ctrls[tabId]?.add(next)` → `tabChatStateProvider` 拿到 → `TodoPanel` rebuild ✓

#### 7. ✅ `_preprocessSubAgent` 内 TodoWrite 检测位置正确（**修复后**）
- 位于 line 614-624，sidechain 分支之后、Task 检测之前
- 命中后**不再 continue**，raw 继续进 `_detectTaskInRecord` + passthrough
- 与 Task 分支保持一致："上游不吞 raw、下游决定渲染"
- 不影响 absorbedByBlock 语义（Task tool_result 吸收逻辑独立）

#### 8. ✅ 子 agent 内 TodoWrite 不进 _detectTodoWrite
- sidechain 分支 line 611 `continue`，永远到不了 line 614 ✓
- 子 agent 内 TodoWrite 仍走 SubAgentFoldedBlock 内 toolUse 渲染 ✓

### C. TodoPanel widget

#### 9. ✅ 空 todos `SizedBox.shrink()`（`todo_panel.dart:33`）
#### 10. ✅ 折叠态文字格式 + 30 字符截断 + 「已完成 ✅」/「待开始」分支（line 87-98）
#### 11. ✅ 展开态三态 icon + strikethrough（line 140-167）
- pending → `radio_button_unchecked` + `textMuted`
- inProgress → `timelapse` + `accent`
- completed → `check_circle` + `success`
- strikethrough on completed + decorationColor 正确

#### 12. ✅ AnimatedSize 200ms easeInOut + 点击 header 切换（line 56-58, 100-101）

### D. chat_screen.dart 嵌入

#### 13. ✅ TodoPanel 嵌入位置（`chat_screen.dart:102-105`）
- 在 `_Header(tab: tab)` 与 `Expanded(child: chatAsync.when(...))` 之间
- 非 Expanded 子项 — todos 空时 0 高度，非空时按内容自然撑开
- 不挤压 MessageCardList、InputBar、ToolProgressIndicator、AskUserQuestionCardRealtime

#### 14. ✅ 不破坏现有布局
- 仅新增一行 `TodoPanel(tabId: widget.tabId)`，未删除/移动现有 widget

### E. 业务规则全覆盖

#### 15. ✅ R-T1-001 主 session only
- sidechain 早 continue，TodoWrite 检测仅对 `isSidechain=false` 生效

#### 16. ✅ R-T1-002 按 tabId 隔离
- `todos` 字段属于 `TabChatState`，`_state` map 以 tabId 为 key

#### 17. ✅ R-T1-003 ~ R-T1-009
- R-T1-003 折叠态文字 ✓
- R-T1-004 三态 icon ✓
- R-T1-005 覆盖式更新 ✓
- R-T1-006 消音不渲染 ✓（**渲染层执行**：message_card_list 跳过 toolName='TodoWrite'）
- R-T1-007 sub-agent 内 TodoWrite 不进主 panel ✓
- R-T1-008 空 todos 不显示 panel ✓
- R-T1-009 30 字符截断 + `…` ✓

#### 18. ✅ R-T1-010 动画
- `AnimatedSize` 200ms easeInOut，纯 layout 动画不拦截 touch event

#### 19. ✅ R-T1-011 历史回放
- `_preprocessSubAgent` 在 msg.stream 和 msg.history.response 两条路径均被调用
- R-T1-005 覆盖式更新 → 重连历史回放后 panel 显示最后一次 TodoWrite 状态 ✓

### F. 编译验证

```
Analyzing AndroidClient...
   info • Invalid use of a private type in a public API • lib/widgets/notification_toast.dart:51:8 • library_private_types_in_public_api
1 issue found. (ran in 1.5s)
```

✅ 仅 1 个 pre-existing info（notification_toast.dart），**未引入任何新的 error/warning/info**。

---

## 处置汇总

| 编号 | 初审等级 | 主 Agent 处置 | 复审验证 |
|---|---|---|---|
| 🟡-1 `_detectTodoWrite` safeguard | 🟡 | A（修复） | ✅ 验证通过 |
| 🟡-2 整条 raw 被吞导致 text 丢失 | 🟡 | A（修复，渲染层消音） | ✅ 验证通过 |
| 🟢-1 注释 doc-comment 归属 | 🟢 | 未处置 | 保留（绿色不阻塞） |

---

## 总结

| 类别 | 数量 |
|---|---|
| 🔴 红色（阻塞） | 0 |
| 🟡 黄色（工程质量） | 2 |
| 🟢 绿色（偏好） | 1 |

**机械校验**：
- 未处置.红 == 0 ✓
- 未处置.黄 == 0 ✓
- 已修复.红 + 升级为红.红 == 总数.红（0 + 0 == 0）✓
- 已修复.黄 + 升级为红.黄 + 入技术债.黄 == 总数.黄（2 + 0 + 0 == 2）✓

**结论**：第一轮代码正确性审查**通过**。两个 🟡 已通过处置 A（修复）解决，且修复方案与 Task 分支模式对齐，可维护性优于初审时的"continue 吞 raw"模式。🟢-1 注释 polish 留为偏好类遗留，不阻塞流程。
