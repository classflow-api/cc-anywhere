# AskUserQuestion 远程交互 - 上线研判报告

## 1. 文档信息

| 字段 | 值 |
|---|---|
| 版本号 | v1.0 |
| 研判日期 | 2026-05-15 |
| 研判 Agent ID | 独立子 Agent（阶段十一上线研判，未参与任何之前阶段） |
| 流程起点 commit | `b752bfdc49ab061bf733a1f4a3f4b5fdfa8ffc72` |
| 流程终点 commit | `b752bfdc49ab061bf733a1f4a3f4b5fdfa8ffc72`（**所有改动仍在 working tree 未 commit**） |
| 流程级别 | L4 |
| 执行模式 | 无人值守 |
| 用户配置 | 生成测试用例：否 / 自动化测试：否 / 无人值守：是 |

> **重要说明**：流程起点与当前 HEAD 是同一 commit；本流程产出的全部代码（4654 行新增文件 + 15 个修改文件，+1049 / -8）均在 working tree，**尚未提交**。本研判把 working tree 改动视作流程产出。

---

## 2. 研判依据

### 2.1 文档清单（按阅读顺序）

| 序号 | 文件 | 状态 |
|---|---|---|
| 1 | `docs/AskUserQuestion远程交互/工作流元信息.md` | ✅ 已读 |
| 2 | `docs/AskUserQuestion远程交互/产品需求文档.md` | ✅ 已读 |
| 3 | `docs/AskUserQuestion远程交互/需求规格说明书.md` | ✅ 已读 |
| 4 | `docs/AskUserQuestion远程交互/技术实施文档.md` | ✅ 已读 |
| 5 | `docs/AskUserQuestion远程交互/第一轮审查报告.md` | ✅ 已读 |
| 6 | `docs/AskUserQuestion远程交互/第二轮审查报告.md` | ✅ 已读 |
| 7 | `docs/AskUserQuestion远程交互/第三轮审查报告.md` | ✅ 已读 |
| — | `测试用例.xlsx` / `验收报告.md` / `自动化测试报告.md` / `无人值守已知问题.md` | 阶段一用户选择"都不需要"，**未生成**，不必读取 |

### 2.2 代码改动统计

- **修改文件**：15 个（Mac 8 + Android 4 + Server 2 + Mac Package.swift 1）
- **新增文件**：15 个（Mac 10 + Android 5 + Python 1）
- **总行数**：+1049 / -8 修改 + ~4654 行新增文件 ≈ **总共约 +5700 行**
- **三端分布**：
  - Mac App（Swift）：约 3050 行新增 + 1049 行修改集中地
  - Android（Dart）：约 1660 行新增 + 多个文件改造
  - Server（Go）：+85 / +14 行（messages.go + router.go 协议常量与路由 case）
  - Python（hook bridge）：248 行单脚本

### 2.3 改动文件清单（关键路径）

**Mac App 新增**：HookProtocol.swift / cc-anywhere-hook-bridge.py / AskQuestionCardController.swift / HookBridgeDeployer.swift / HookIpcAdapters.swift / HookIpcServer.swift / HookPreferencesService.swift / SettingsJsonInstaller.swift / AskQuestionCardView.swift / HookPane.swift

**Mac App 修改**：Package.swift / AppDelegate.swift / CCAnywhereApp.swift / DependencyContainer.swift / ProtocolMessage.swift / JSONLWatcher.swift / ProcessHost.swift / MainWindow.swift / PreferencesWindow.swift

**Android 新增**：ask_user_question_card_realtime.dart / ask_question_controller.dart / dedup_service.dart / notification_toast.dart / tool_progress_indicator.dart

**Android 修改**：app.dart / chat_repository.dart / chat_screen.dart / protocol_message.dart

**Server**：protocol/messages.go / router/router.go

---

## 3. 六维度风险评估表

| 维度 | 评估摘要 | 风险等级 |
|---|---|---|
| **D1 · 三轮 Review 状态** | 第一轮 4 🔴 + 第二轮 2 🔴 全部已修，复审均通过；第三轮初审 0 🔴，遗留 5 🟡 + 5 🟢 全部非阻塞。代码静态质量可控；但部分 🟡 涉及生产可观测性（日志缺 tabId/toolName 上下文）与 Notification schema 字段映射可疑（颜色区分语义可能失真），上线后需关注。 | 🟡 |
| **D2 · 自动化测试状态** | 阶段一用户明确选择"都不需要"，未生成测试用例.xlsx、未跑自动化测试。本需求**触及 hook bridge ↔ 全局 settings.json 写入 ↔ 三端 ws 协议 ↔ Claude SDK 外部进程协议**四个高耦合点，无任何机器自动化验证保障；第三轮 Review 已专门标注"建议上线后第一个迭代补 SettingsJsonInstaller + HookIpcServer 单元测试"。该缺口对 hook 桥接基础设施类需求是重要短板。 | 🔴 |
| **D3 · 高风险区域改动** | 触及五类高风险点：① `~/.claude/settings.json` 全局用户配置（虽 backup + 精准前缀卸载）；② 全局 hook 全 Claude 子进程生效（Mac App / Terminal / 其他 plugin 共存）；③ Claude SDK PreToolUse hook 协议（外部 API 协议依赖）；④ env CC_ANYWHERE_TAB_ID 注入所有 claude 子进程；⑤ JSONLWatcher 改造增加去重过滤，影响既有 phone 历史消息推送路径。即便软失败保护齐全，仍是"全用户全 Claude 会话生效"的写入级改动。 | 🔴 |
| **D4 · 仅人工可验证场景** | 大量关键路径**完全没有验证**：① **T12 echo 二次验证（updatedInput.answers 是否真被 SDK 采纳）尚未执行** — PRD 风险 R1 / DG-1 明确"未通过则升级方案 A"，目前只完成 hook 链路通达验证；② **T18 三端 E2E 联调全部场景**（10 个场景表）未执行；③ Mac App 首次启动许可弹窗 UX；④ Phone 真机上 tool_approval 红色徽章/允许拒绝按钮渲染；⑤ 多 phone 竞答 winner 锁真实网络环境；⑥ 用户终端直接跑 claude 不被干扰（NFR-U2 硬规则）；⑦ M4 ON 后 Bash/Write/Edit 远程批准全链路；⑧ inner timeout 5 min fallback 后 TUI 弹原问题；⑨ 偏好关闭主开关后 settings.json 完整卸载（不误删其他 plugin hooks）；⑩ Notification 真实 stdin schema 字段映射验证。 | 🔴 |
| **D5 · RTM 覆盖完整性** | 无 RTM 文件可查（测试用例.xlsx 未生成）。以第一轮 Review 的"业务规则覆盖矩阵"作为替代评估：39 条业务规则中初审 35 条 ✅ + 4 条 🔴；第一轮复审后全部修复，第二轮第三轮无规则项倒退。**静态规则覆盖率 100%**，但**所有规则均无自动化执行证据**（D2/D4 短板叠加）。 | 🟡 |
| **D6 · 已知问题影响** | 第三轮遗留 5 🟡 + 5 🟢，均非上线阻塞但影响范围逐条分析见 §4。其中 #3（日志缺 tabId/toolName）、#5（Notification 字段映射可疑导致 M3 颜色退化）、#4（socket chmod 0600 异步轮询 2s 窗口）对生产可观测性与安全有轻度影响；M4 deny reason 未在 UI 收集（第二轮复审残留 2）属 P2 体验缺口。T12 / T18 未执行的实证缺失是最大风险。 | 🟡 |

---

## 4. 已知问题逐条影响分析

### 4.1 第三轮 Review 5 个 🟡 警告

| 序号 | 来源 | 问题描述 | 影响面 | 是否阻塞上线 |
|---|---|---|---|---|
| W1 | 第三轮 #1 | `DependencyContainer.handleAskToolApprovalInbound` 仍用裸 dict 解析，未迁强类型 `AskToolApprovalAnswerPayload` | 代码可读性 / 风格一致性；功能等价无差异 | 否 |
| W2 | 第三轮 #2 | `HookIpcServer.handleAsk` 单方法 75 行 + actor 整体 740 行职责偏胖（5 件事揉一起） | 后续迭代可维护性下降；当前功能正确 | 否 |
| W3 | 第三轮 #3 | `reapExpired` / `resolveAsk` 日志缺 tabId / toolName 上下文 | **生产排查效率**：上线后若用户反映"AskUserQuestion 卡 5 分钟"，仅靠日志无法定位单条 | 否（但**强烈建议上线前补**） |
| W4 | 第三轮 #4 | socket chmod 0600 异步轮询 2s 窗口期内 socket 文件可能以默认权限存在 | **安全 NFR-S1 弱化**：同 host 同用户其他进程理论可在窗口内 connect。个人工具定位 + 同用户场景下风险确实低 | 否（但建议同步化） |
| W5 | 第三轮 #5 | Python `cmd_notification` 字段抽取未对齐 Claude 真实 Notification schema | **R-F3-003 颜色区分语义失真**：phone NotificationToast 永远拿默认 `notification_type="idle"` 灰色 | 否（M3 功能仍可用，仅颜色单一） |

### 4.2 第三轮 Review 5 个 🟢 建议

| 序号 | 来源 | 问题描述 | 影响面 | 是否阻塞上线 |
|---|---|---|---|---|
| S1 | 第三轮 #6 | DedupService 持久化 corrupt 时未主动清理 | 启动时多一次冗余 jsonDecode，in-memory 兜底正确 | 否 |
| S2 | 第三轮 #7 | `makeCommandString` 路径含 `"` 时未转义 | 用户 home 路径含 `"` 极罕见；理论可能但实际概率极低 | 否 |
| S3 | 第三轮 #8 | `parseToolResponse` 缺省字段语义不严格 | progress.post 失败边界场景 phone 不展示红 toast | 否 |
| S4 | 第三轮 #9 | Server 端缺新 type 命中频率 metrics | 生产观测面缺失，日志 grep 兜底 | 否 |
| S5 | 第三轮 #10 | `HookPreferencesService` 三个 didSet 模板重复 | 代码冗余 ~3 行，仅 3 个开关时收益有限 | 否 |

### 4.3 第二轮复审残留两条提示

| 序号 | 来源 | 问题描述 | 影响面 | 是否阻塞上线 |
|---|---|---|---|---|
| R1 | 第二轮复审残留 1 | `handleAskToolApprovalInbound` 风格不统一（同 W1） | 与 W1 重叠，代码风格 | 否 |
| R2 | 第二轮复审残留 2 | `ask_question_controller.submitApproval` 未透传 reason 字段；UI 也未提供"附加拒绝原因"输入入口（R-F4-005 P2 功能缺口） | M4 拒绝时无法附带 reason 反馈给 Claude，Claude 不知道为何被拒，可能影响后续 turn 决策 | 否（M4 默认 OFF + 1 周灰度观察） |

### 4.4 第一轮复审残留 R1（文档自相矛盾）

| 序号 | 来源 | 问题描述 | 影响面 | 是否阻塞上线 |
|---|---|---|---|---|
| D1 | 第一轮复审残留 R1 | 需求规格说明书.md:342 元素表仍写"默认 ON"，与代码（默认 OFF）和场景 F6-S1（默认 OFF）矛盾 | 文档级跟进，代码已对齐 PRD | 否 |

### 4.5 T12 / T18 未执行（最重要）

| 序号 | 来源 | 问题描述 | 影响面 | 是否阻塞上线 |
|---|---|---|---|---|
| **T12** | 技术实施文档 §8.4 阶段七关键 | **echo 实验二次验证 `updatedInput.answers` 是否真被 SDK 采纳** 推迟到研判后 | **PRD 风险 R1 命中概率"中"、影响"高"**：若 SDK 不采纳，M1 整个 AskUserQuestion 实时交互功能失效，需要切方案 A（SDK 路线）3-5 人日重做 | **是 — 必须先验证** |
| **T18** | 技术实施文档 §8.5 验证矩阵 | **三端端到端集成测试**（10 个场景全表）未执行 | **6 类 P0 场景** + **4 类边界场景** 全部无验证证据；多 phone 竞答、5 min timeout fallback、终端直接跑 claude 不被干扰、settings.json 不误删其他 plugin、M4 ON 下 Bash 远程批准全链路 — 任何一项首次上线翻车风险都不可控 | **是 — 必须先验证** |

---

## 5. 必须人工验证的场景

| 序号 | 场景描述 | 验证方式 | 工作量评估 |
|---|---|---|---|
| **V1** | **T12 — echo 实验二次验证：在 cc-anywhere Tab 实际触发 AskUserQuestion，phone/Mac 答完后观察 Claude SDK 是否真采纳 `updatedInput.answers`（不再弹 TUI 内置 AskUserQuestion 弹窗，直接 emit tool_result 落 JSONL）** | 启动 Mac App → 开新 Tab → 跑 claude → prompt 触发 AskUserQuestion → phone 答 → 看 TUI 与 JSONL | **0.5 人日**（含 SDK 不采纳的后续调研） |
| V2 | T18-1 单 phone 在线，问题正常答 → Claude 继续 | 手工三端联调 | 0.2 人日 |
| V3 | T18-2 phone 离线，Mac 端答 | 关 phone → Mac 答 | 0.1 人日 |
| V4 | T18-3 phone 选 Other 文字自定义回答 | phone 点自定义输入 | 0.1 人日 |
| V5 | T18-4 多 phone 同时回答 winner 锁仲裁（首回复 wins，其他端显示已被回答） | 双 phone 同时上线，几乎同时点选项 | 0.3 人日 |
| V6 | T18-5 inner timeout 5 min → hook 返回 deny → Claude TUI 弹原问题 fallback | 5 分钟不操作；观察 TUI fallback 行为；同时确认 phone 端补拉 JSONL 历史能看到本次 AskUserQuestion 记录（验证第二轮 #2 修复） | 0.3 人日 |
| V7 | T18-6 用户终端直接跑 claude（无 cc-anywhere），hook bridge 0 干扰（NFR-U2 硬规则） | 关 Mac App → 终端 `claude` → 触发 AskUserQuestion → TUI 正常弹窗 | 0.1 人日 |
| V8 | T18-7 偏好关闭主开关 → settings.json cc-anywhere 条目精准卸载，**其他 plugin hooks 不被误删**（NFR-S6） | 先安装 yoolines-dev-workflow / superpowers 等 plugin 让 settings.json 含多 plugin hooks → 然后关闭 cc-anywhere 主开关 → diff 验证 | 0.2 人日 |
| V9 | T18-8 M4 ON 后 Bash 调用，phone 弹"⚠ 工具批准"红色徽章 + 允许/拒绝按钮 → 选拒绝 → Claude 不执行 | 开 M4 toggle → 让 Claude 跑 `bash -c "ls"` → phone 拒绝 | 0.2 人日 |
| V10 | T18-9 M4 OFF 后 Bash 调用仅显示 progress 不批准 | 关 M4 toggle → 同 V9 流程 → phone 只看到 progress | 0.1 人日 |
| V11 | T18-10 hook bridge 人为破坏 socket 路径，Claude 不卡死走 fallback（NFR-U1 硬规则） | `rm ~/Library/Application Support/cc-anywhere/hook.sock` 后跑 Claude | 0.2 人日 |
| V12 | Mac App 启动后首次许可弹窗 UX（NSAlert 文案与按钮交互） | 删除 plist 偏好 + 启动 Mac App → 切 toggle ON → 看许可弹窗 | 0.1 人日 |
| V13 | Phone 真机渲染 tool_approval 卡片（红色徽章 + tool_input 摘要 200 字符截断） | 真机连测试服务器 | 0.1 人日 |
| V14 | Notification 真实 stdin schema 验证（W5 / 第三轮 #5） | 真实触发 Claude Notification（idle / permission_prompt / error）抓 hook bridge stdin | 0.3 人日 |

**预估总工作量：约 3 人日**（其中 V1 是 go/no-go 闸门，必须最先做）

---

## 6. 最终结论

- [ ] ✅ 可直接上线，无需任何额外测试
- [ ] ⚠️ 可上线但建议补充测试
- [x] **🛑 必须先完成以下人工测试才能上线**
- [ ] ❌ 不建议上线

### 6.1 结论摘要

**结论：🛑 必须先完成人工测试才能上线。**

理由：

1. **D2 自动化测试 🔴 + D3 高风险区域 🔴 + D4 仅人工可验证场景 🔴 三项叠加**：本需求是 hook 桥接基础设施改动，涉及全局 settings.json 写入 + Claude SDK 外部协议 + 三端 ws 协议新增 7 个 type，但全程无任何自动化测试覆盖，所有关键链路均依赖人工验证。

2. **T12 echo 二次验证未执行**：PRD §7.1 R1 风险（SDK 不采纳 `updatedInput.answers`）概率"中" / 影响"高"。如果 SDK 不采纳，M1 功能直接失效。**这是技术实施文档明确定义的"go/no-go 闸门"**，未通过禁止继续。

3. **T18 三端 E2E 联调未执行**：技术实施文档 §8.5 的 10 个验证场景全部无证据，包括 NFR-U1/U2 软失败硬规则（终端直接跑 claude / hook bridge 破坏）这类生产关键路径。

4. **代码质量已可控**：三轮 Review 共 6 🔴 全部修复，遗留 🟡 / 🟢 均非阻塞；代码层面已经具备上线条件，**纯粹缺人工实证**。

5. **回滚保险完备**：SettingsJsonInstaller 提供 backup（5 份）+ 精准前缀卸载 + atomic rename，git revert 也可清理；即便上线后翻车，回滚成本可控。

### 6.2 必须完成的人工验证清单（按优先级）

**第一闸门（必须先过，否则全部白做）**：
- [ ] **V1**：T12 echo 二次验证 — 验证 SDK 是否采纳 `updatedInput.answers`

**核心验证（V1 通过后并行）**：
- [ ] V2 单 phone 在线正常答题
- [ ] V6 inner timeout 5min fallback + phone 端 JSONL 历史可见
- [ ] V7 用户终端直接跑 claude 0 干扰（NFR-U2）
- [ ] V8 偏好关闭主开关不误删其他 plugin（NFR-S6）
- [ ] V11 hook bridge 破坏后 Claude 不卡死（NFR-U1）

**功能完整性验证**：
- [ ] V3 phone 离线 Mac 端答
- [ ] V4 phone Other 自定义文字回答
- [ ] V5 多 phone 竞答 winner 锁
- [ ] V12 首次许可弹窗 UX
- [ ] V13 Phone 真机 tool_approval 卡片渲染

**M4 灰度前验证（M4 默认 OFF，可与主功能并行验证后再放开）**：
- [ ] V9 M4 ON Bash 远程批准全链路
- [ ] V10 M4 OFF Bash 仅 progress 不批准

**可与上线并行的优化项**（不阻塞主上线）：
- [ ] V14 Notification 真实 schema（影响 M3 颜色区分语义；可上线后补）

### 6.3 上线前强烈建议同步修复（可不阻塞，但 ROI 高）

1. **W3 日志补 tabId/toolName 上下文**：生产排查必备
2. **R2 / 文档残留 D1**：M4 deny reason 收集入口 + 需求规格说明书 §3.1 F6 默认 OFF 修订（5 分钟工作量）
3. **W5 Notification schema 兼容性**：先做嵌套 + 顶层双兜底（不影响 M3 上线）

### 6.4 灰度建议

按 PRD §7.5：
- **M1-M3 默认 OFF（首启零侵入）**，由用户主动 OFF→ON 触发首次许可弹窗；上线即生效，无强制灰度需求
- **M4 默认 OFF**，1 周观察 hook bridge 错误日志频率 + 用户反馈 后再决定是否默认开启

---

## 7. 研判签名

- **研判 Agent**：独立子 Agent（阶段十一上线研判，未参与阶段一至阶段十的任何工作）
- **研判时间**：2026-05-15
- **研判依据 commit**：`b752bfdc49ab061bf733a1f4a3f4b5fdfa8ffc72`（流程起点 = 当前 HEAD；working tree 内 +1049 / -8 修改 + 15 个新增文件约 4654 行均视作流程产出）
- **本研判仅基于上述文档与代码静态阅读做出，不受任何主 Agent 上下文影响**
- **关键判定**：T12（SDK 是否采纳 updatedInput.answers）是 go/no-go 硬闸门；未过则 M1 失效需切方案 A
