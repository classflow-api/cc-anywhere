---
review轮次: 第二轮-边界值与业务影响
适用skill: yoolines-dev-workflow
状态: 通过
总数: {红:0, 黄:0, 绿:1}
已修复: {红:0, 黄:0, 绿:0}
升级为红: {黄:0}
入技术债: {黄:0}
未处置: {红:0, 黄:0}
重跑次数: 0
---

# 第二轮代码审查报告 — AskQuestionCard 翻页改造

## 审查范围

- 文件：`MacClient/Sources/CCAnywhere/Views/AskQuestionCardView.swift`
- 业务关联文件（grep 验证不受波及）：
  - `MacClient/Sources/CCAnywhere/Services/AskQuestionCardController.swift`
  - `MacClient/Sources/CCAnywhere/Services/HookIpcServer.swift`
  - `MacClient/Sources/CCAnywhere/Models/HookProtocol.swift`（AskCardRequestData / AskQuestionItem）
  - `MacClient/Sources/CCAnywhere/Models/ProtocolMessage.swift`（AskQuestionPendingPayload 等）
  - `MacClient/Sources/CCAnywhere/Views/TabContentView.swift`（嵌入位置）
  - `MacClient/Sources/CCAnywhere/Views/MainWindow.swift`（验证 TabContentView 的实例化模式）
- 关联架构：`docs/ARCHITECTURE.md` §3.1（双通道）/ §3.5（Winner 锁）/ §3.7（协议）
- 关联文档：`docs/AskQuestionCard翻页改造/变更说明.md`、`docs/AskQuestionCard翻页改造/第一轮审查报告.md`
- 本轮目标：边界值与业务影响。不重复第一轮已通过的逻辑正确性检查。

## 检查清单逐条结论

### A. 数据边界

| # | 检查项 | 结论 | 备注 |
|---|--------|------|------|
| 1 | questions 集合为空（N=0） | ✓ | `currentQuestion` getter（line 318-321）`guard currentIndex >= 0, currentIndex < questions.count else { return nil }` 兜底；body 中 `if let q = currentQuestion { questionBlock(...) }` 不渲染 questionBlock；progressIndicator 由 `showStepper = totalCount >= 2` 控制不显示；navigationBar 走 else 分支只显示 submitButton；submitButton 由 `canSubmit`（line 535 `guard !qs.isEmpty else { return false }`）禁用；`.onKeyPress(.return)` 因 `canSubmit=false` 不触发提交。所有路径已有越界保护，无越界风险。 |
| 2 | questions 单元素（N=1） | ✓ | `showStepper = false`（line 272），progressIndicator 不显示；navigationBar 走 else 分支只显示 submitButton（line 380-383）；onAppear 不抢焦点（line 290 `if showStepper`）；`isLast=true`（max(0,0)=0，currentIndex 0 ≥ 0 = true）；goPrev/goNext 都因 `isFirst==isLast==true` 提前 return；Enter 触发提交需用户先点击卡片激活 keyboardFocused — SwiftUI 焦点协作模型正常工作，单题视觉与现有完全一致。 |
| 3 | questions 超大（如 N=100） | ✓ | SwiftUI 仅渲染 currentQuestion 一题 + navigationBar + progressIndicator；翻页只切 currentIndex，`.id(currentIndex)` 重建 questionBlock 子树；@State 字典容量 ≤ N，正常；`.animation(.easeInOut(duration: 0.15), value: currentIndex)` 仅轻量 alpha/translation 过渡。AskUserQuestion 工具的 schema 实务上 questions ≤ 4，N=100 仅为理论上限，无性能风险。 |
| 4 | option 列表为空 | ✓ | `ForEach(Array(question.options.enumerated()))` 不渲染；otherSentinel optionRow 仍渲染（line 426-432，R-F1-012）；canSubmit 校验 set 非空 + otherSentinel 选中时 otherTexts 非空，符合预期：用户必须输入自定义文本才能提交。 |
| 5 | 自定义输入项边界 | ✓ | otherTexts trim 后空 → canSubmit=false（line 540-542）；含空格 trim 后空 → canSubmit=false；200 字符可输入；201 字符截断到 200（line 483-484 `newValue.prefix(Self.otherMaxLength)`）；emoji/中文按 Swift String.count（grapheme cluster）计数，与现状一致（line 483-485 本次未改）。 |

### B. 状态边界

| # | 检查项 | 结论 | 备注 |
|---|--------|------|------|
| 6 | 快速翻页并发 | ✓ | @State 写入由 SwiftUI 主线程序列化；goPrev/goNext 各自 `guard !isFirst/!isLast`，currentIndex 只递增/递减 ±1，无 race；onKeyPress 由 SwiftUI 主线程派发，连续按 ← → 会按顺序执行。 |
| 7 | 翻页中 winner 锁触发 | ✓ | HookIpcServer.resolveAsk 触发 → cardController.dismiss → pendingByTab[tabId] 移除 → AskQuestionCardView 的 `pending = controller.pending(forTab: tabId)` 变 nil → `if let req = pending {}` 为 false → CardOverlay 销毁 → UserQuestionBody @State（含 currentIndex / selections / otherTexts）全部销毁 → AnsweredBanner 弹出 3 秒。view 树销毁正常，currentIndex 不会残留干扰下一个 request。 |
| 8 | 多 Tab 卡片切换 | △ 信息项 | MainWindow.swift:40 `TabContentView(tab: tab)` **未加 `.id(tab.id)`**（与 line 39 `FileExplorerView(...).id(tab.id)` 对比明显）— TabContentView 与内嵌的 AskQuestionCardView 是 SwiftUI 结构身份复用同一实例，参数 tabId 切换。切到无 pending 的 tab B → `pending=nil` → CardOverlay 销毁 → UserQuestionBody @State 全部丢失；切回 tab A → pending 重现 → CardOverlay/UserQuestionBody 重建 → currentIndex/selections/otherTexts 重置。**这是变更说明 §5 风险表明确声明的低风险项（"@State 由 SwiftUI 自动持久化在 View 生命周期内，与现有 selections 一致"），本次仅把新引入的 currentIndex 加入同样行为，无放大问题。pre-existing 不在本轮处置范围。** 详见"发现的问题 #1（绿色 信息项）"。 |

### C. 业务影响

| # | 检查项 | 结论 | 备注 |
|---|--------|------|------|
| 9 | 协议兼容性 | ✓ | onSubmit 闭包签名 `[String: String] -> Void` 未变（line 250 vs 旧代码）；`AskCardRequestData / AskQuestionItem`（HookProtocol.swift line 170-195、ProtocolMessage.swift line 219-244）未触；`AskQuestionCardController.submitUserQuestion(requestId:answers:)` 入口签名未变（git diff AskQuestionCardController.swift 无变化 — 该文件不在 git status 修改列表中）。 |
| 10 | JSONL 历史兼容性 | ✓ | `grep "ask.question\|AskQuestion" JSONLWatcher.swift` 无任何匹配 — JSONLWatcher 完全不感知 ask 卡片流程，本次纯 view 层改造对 JSONL 解析格式零影响。 |
| 11 | 手机端协议兼容性 | ✓ | mac 翻页是纯本地 @State 切换 currentIndex，没有任何 ws 消息发送路径（grep `controller\|server\|sendAsk\|ws` 在 UserQuestionBody 函数体内的引用只有 submit() 一处，通过 onSubmit 闭包走 controller.submitUserQuestion，未在翻页路径触发）。手机端 `ask_user_question_card_realtime.dart` 在 mac 翻页过程中不会收到任何新消息，行为零影响。 |
| 12 | tool_approval 分支不受影响 | ✓ | CardOverlay 中 `if request.askKind == "tool_approval"` 走 ToolApprovalBody（line 150-156，未改）；user_question 才走 UserQuestionBody。grep 验证 ToolApprovalBody 的实现（line 169-243）与 git diff 在 ToolApprovalBody 内的改动为 0。工具批准卡片完全无回归。 |
| 13 | mac 主动取消 / 关闭 | ✓ | 卡片仍不允许主动取消（变更说明 §1.2 注释、文件顶部 line 24-25 注释），onKeyPress 未引入 `.escape`/`.cancel`；submit 仅在 canSubmit 时调用 onSubmit；翻页只改 currentIndex 不触发 onSubmit；本次改造未引入任何意外关闭路径。 |
| 14 | 超时回收（30 分钟） | ✓ | HookIpcServer.reapExpired → cardController.dismiss(reason: .timeout) → pendingByTab 移除 → view 树销毁，与现状一致。HookIpcServer.swift:446-485 reapExpired 实现未触动。 |

### D. 影响范围（对现有模块）

| # | 检查项 | 结论 | 备注 |
|---|--------|------|------|
| 15 | AskQuestionCardController 是否需要改 | ✓ | git status 显示 `AskQuestionCardController.swift` 不在 modified 列表中（仅本文件 AskQuestionCardView.swift 改）；其 pending/answered/submit 接口完全未触；本次改造严格遵守"零行为变化"（变更说明 §2.2 设计原则）。 |
| 16 | HookIpcServer 是否需要改 | ✓ | git status 显示 HookIpcServer.swift 虽在 modified 列表（其他改动），但 grep `UserQuestionBody\|currentIndex\|selections\|otherTexts` 在 HookIpcServer.swift 中匹配 **0 处** — winner 锁、resolveAsk、reapExpired 与 view 翻页 state 完全解耦。 |

## 发现的问题

| 序号 | 严重程度 | 文件:行号 | 问题描述 | 修复建议 | 处置状态 |
|-----|---------|----------|---------|---------|---------|
| 1 | 🟢 | MainWindow.swift:40 + AskQuestionCardView.swift:253-260（UserQuestionBody @State） | **多 Tab 切换时 UserQuestionBody @State 复位（pre-existing 信息项）**：TabContentView 在 MainWindow 中未加 `.id(tab.id)`，而 SwiftUI 通过结构身份复用同一 View 实例。切到无 pending 的 tab 时 CardOverlay 销毁，再切回会重建 → currentIndex / selections / otherTexts 重置为初值。**本次新引入的 currentIndex 与现有 selections / otherTexts 行为一致**（变更说明 §5 风险表明确声明）。 | 无需修复（纯主观偏好，pre-existing；如需保留，需把 @State 上提到 Controller 层，跨多文件改动，远超 L3 范围）。 | 信息项记录 |

注：本项作为🟢（纯主观偏好 / pre-existing）记录，不计入处置队列。变更说明 §5 已显式承认此为"低风险，与现有 selections 一致"。

## 本轮是否引入新问题

无。

详细核对：

- 翻页 state 的引入未破坏 winner 锁仲裁（HookIpcServer 完全不感知 view internal state）
- 翻页 state 的引入未改变协议层任何字段
- 翻页 state 的引入未触发任何 ws 消息发送路径
- 翻页 state 的引入未影响 ToolApprovalBody 工具批准卡片
- 翻页 state 的引入未引入卡片主动取消路径
- N=0 / N=1 / N=大 三个边界全部有保护或退化
- 多 Tab 切换 @State 复位是现有行为的延续，不放大问题

## 审查结论

- [x] **通过**：0 个 🔴 + 0 个 🟡 + 1 个 🟢（信息项），未处置数为 0
- [ ] 不通过

机械校验：

- `状态: 通过` ✓
- `未处置.红 == 0` 且 `未处置.黄 == 0` ✓
- `已修复.红 (0) == 总数.红 (0)` ✓
- `已修复.黄 (0) + 升级为红.黄 (0) + 入技术债.黄 (0) == 总数.黄 (0)` ✓

🟢 项为信息记录，不计入处置队列。

主 Agent 可推进至**第三轮审查（代码质量与生产就绪）**。
