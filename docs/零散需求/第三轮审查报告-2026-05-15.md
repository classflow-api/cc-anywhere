# 第三轮代码审查报告 - 代码质量与生产就绪

> 审查日期:2026-05-15
> 审查范围:零散需求修改报告中列出的全部文件 + 第二轮复审报告中已修复 / 遗留的项
> 审查重点:代码质量(命名/职责/重复)、错误处理、日志、安全性、性能、资源管理、测试覆盖、协议风格一致性、回滚方案
> 审查方法:独立打开修改后代码,代码级走查 + 跨端协议字段对照 + 资源生命周期推演;不读其它 Agent 对话历史。

---

## 审查范围

### Mac 端
- `MacClient/Sources/CCAnywhere/App/AppDelegate.swift`(需求 1/34)
- `MacClient/Sources/CCAnywhere/App/DependencyContainer.swift`(需求 33)
- `MacClient/Sources/CCAnywhere/Services/WSClient.swift`(需求 2/7)
- `MacClient/Sources/CCAnywhere/Services/ImageDownloader.swift`(需求 13)
- `MacClient/Sources/CCAnywhere/Services/InputInjector.swift`(需求 14、🔴 #1 修复)
- `MacClient/Sources/CCAnywhere/Services/SlashCommandBridge.swift`(需求 33)

### Server 端
- `Server/internal/server/server.go`(需求 10/17)
- `Server/internal/image/image.go`(需求 16/17)
- `Server/internal/protocol/messages.go`(需求 17/33)
- `Server/internal/router/router.go`(需求 33)

### Android 端
- `AndroidClient/lib/main.dart`(需求 20)
- `AndroidClient/lib/models/server_config.dart`(需求 3)
- `AndroidClient/lib/models/protocol_message.dart`(需求 17/33)
- `AndroidClient/lib/models/message.dart`(需求 4/8/22/30/32/27/20/29/37)
- `AndroidClient/lib/data/chat_repository.dart`(需求 5/9/15/18/19/20/23/28/31/32/35)
- `AndroidClient/lib/data/image_upload_service.dart`(需求 19)
- `AndroidClient/lib/data/image_ref_store.dart`(需求 18)
- `AndroidClient/lib/data/tab_repository.dart`(需求 9)
- `AndroidClient/lib/data/slash_command_repository.dart`(需求 33)
- `AndroidClient/lib/routes/app_router.dart`(需求 6)
- `AndroidClient/lib/features/auth/manual_input_screen.dart`(需求 3)
- `AndroidClient/lib/features/settings/settings_screen.dart`(需求 3)
- `AndroidClient/lib/features/chat/chat_screen.dart`(需求 25/35)
- `AndroidClient/lib/features/chat/widgets/input_bar.dart`(需求 12/26/27/33)
- `AndroidClient/lib/features/chat/widgets/attachment_card.dart`(需求 21/27)
- `AndroidClient/lib/features/chat/widgets/assistant_text_card.dart`(需求 36)
- `AndroidClient/lib/features/chat/widgets/message_card_list.dart`(需求 24/37)
- `AndroidClient/lib/features/chat/widgets/ask_user_question_card.dart`(需求 37)

---

## 已确认修复(回放第二轮复审报告中的修复)

| 项 | 状态 | 代码位置 |
|---|---|---|
| 🔴 #1 同名 filename 三处覆盖 | ✅ 修复有效 | InputInjector.swift:61-66 + ImageDownloader 写入 `<uploadId>.<ext>`;chat_repository.dart:215-249 上传成功升级 attachment.filename 形式与 Mac 一致 |
| 🟡 /clear 后 typing 永挂 | ✅ 修复有效 | chat_repository.dart:589-604 _clearTabMessages 内显式 `assistantTyping: false` + cancel timer |
| 🟡 typing 无 60s 超时兜底 | ✅ 修复有效 | chat_repository.dart:578-591 _setTyping 启动 60s Timer 自动复位 |
| 🟡 dotfile 提取语义两端不一致 | ✅ 修复有效 | chat_repository.dart:225-226 用 `dot > 0` 与 Mac NSString.pathExtension 行为对齐 |
| 🟡 _mergeMessages orphan typing timer | ✅ 修复有效 | chat_repository.dart:569-571 改走 _setTyping(false) 触发 timer cancel |
| 🟡 dispose 未 cancel typingTimers | ✅ 修复有效 | chat_repository.dart:621-626 dispose 内 cancel + clear |

---

## 发现的问题

### 阻塞级(🔴)

| 序号 | 严重程度 | 文件:行号 | 问题描述 | 修复建议 |
|-----|---------|----------|---------|---------|
| — | — | — | 本轮**未发现新增阻塞问题**。 | — |

### 警告级(🟡)

| 序号 | 严重程度 | 文件:行号 | 问题描述 | 修复建议 |
|-----|---------|----------|---------|---------|
| 1 | 🟡 警告 | `AndroidClient/lib/main.dart:13-23` + `data/ws_client.dart`(trustSelfSigned 字段)+ `image_upload_service.dart:240-247` | **HttpOverrides.global 信任所有自签证书,与 Settings 页 trustSelfSigned 开关语义割裂(安全)**。需求 3 让用户能在 Settings 里关掉 `trustSelfSigned`,但需求 20 在 main.dart 装了全局 HttpOverrides,无视该开关。结果:用户在公共 WiFi 关掉开关后,Image.network 仍信任任何证书,中间人可拦截图片下载。私有工具场景下风险可控,但 footgun 不该留。 | (a) HttpOverrides.global 内部读 currentConfig.trustSelfSigned 决定是否回退到默认校验;(b) 或彻底移除 HttpOverrides,改用 Image.network + cached_network_image 的自定义 HttpClient(需自定义 ImageProvider)。 |
| 2 | 🟡 警告 | `MacClient/.../Services/ImageDownloader.swift:18-20, 59-69` + `WSClient.swift:286-296` + `AndroidClient/lib/main.dart:13-19` | **`trustAll/badCertificateCallback` 三处独立实现,缺统一开关/审计(重复代码 + 安全语义碎片化)**。Mac WSClient `trustSelfSigned` 是用户配置开关,Mac ImageDownloader 是"始终信任"硬编码(注释"私有工具场景"),Android main.dart 是全局 trustAll。三端三个不同信任模型,没有统一开关,后期审计/收紧策略时无人维护。 | (a) 把 Mac `TrustAllDelegate` 改成 `TrustSelfSignedDelegate` 读 `PreferencesService.trustSelfSigned`,与 WSClient 走同一开关;(b) Android HttpOverrides 同样接读 trustSelfSigned;(c) 文档 docs/ 加 `信任策略.md` 把这三处明示。 |
| 3 | 🟡 警告 | `AndroidClient/.../data/chat_repository.dart:178, 354-359, 376-386` | **dead code 累积内存泄漏 — `_recordLocalPendingAttachment` 在 sendTextWithImages line 178 仍被调用,但消费方 `_consumeMatchingPendingAttachment` 从无调用方(被 line 489 鲁棒版 dedup 取代)**。修改报告"已知待办"已声明,第一/二轮均识别,第二轮复审报告 🟡 #4 标"必修",但**本轮发现仍未修**。后果:用户长会话内多次发图,`_localPendingAttachments[tabId]` 单调增长(120s 窗口仅 put 时清,1 小时不发图则累积 1h 旧记录)。 | 删除 line 178 调用 + line 354-359 `_recordLocalPendingAttachment` 方法 + line 376-386 `_consumeMatchingPendingAttachment` 方法 + line 343-344 `_localPendingAttachments` 字段。 |
| 4 | 🟡 警告 | `AndroidClient/.../data/chat_repository.dart:596-604` | **`_clearTabMessages` 未清 `_pendingDownloadUrl` 跨 tab map,长时间使用累积内存**。第一/二轮均识别,第二轮复审报告 🟡 #5 标"必修",但**本轮发现仍未修**。/clear 触发时 in-flight `image.download.url` 请求 entry 留在内存,后续 server 响应回来虽然 line 400 indexWhere=-1 不崩,但 map 永远不释放。多 tab 反复 /clear 累积。 | line 596-604 内加:`_pendingDownloadUrl.removeWhere((_, v) => v.tabId == tabId);` |
| 5 | 🟡 警告 | `AndroidClient/.../data/chat_repository.dart:307, 393-396` | **image.download.url.response 携带空 URL 时 attachment 卡片永远 broken_image,无错误信息也无重试**。`if (url == null \|\| url.isEmpty) return;` 后 `_pendingDownloadUrl[uploadId]` 已被前一行 remove,后续重试不会再发请求。用户视角 = broken_image 无错误提示也无重发入口。 | response 带空 URL 时,把对应 message 的 `uploadError = '图片已过期'`,让 attachment_card 渲染明确错误 + 可选"重发"按钮。 |
| 6 | 🟡 警告 | `AndroidClient/.../data/chat_repository.dart:548-558` + 复审 #5 关联 | **`_mergeMessages` 末尾对每条 attachment 都重发 `image.download.url` 请求(history reload 时 N×攻击)**。loadHistory 50 条历史 attachment 时,for loop 对每条都触发 `_requestImageDownloadUrl`。即便 `m.attachmentRemoteUrl == null` 防住已有 URL 的情况,在 JSONL 解析出的 attachment 全都无 remoteUrl,所以 50 条全部请求。用户切 tab 反复 loadHistory 时洪流。 | (a) `_pendingDownloadUrl` 内已有该 uploadId 待响应时跳过;(b) 给 download.url 加 in-memory LRU cache,N 秒内重复请求复用上次结果。 |
| 7 | 🟡 警告 | `AndroidClient/.../data/chat_repository.dart:285-330` + 第一/二轮 #15/#22 关联 | **ImageRefStore 丢失/重装后历史图片全黑(缺 fallback)**。`_requestImageDownloadUrl` 查 store 拿不到 uploadId 就直接 `return`(line 317)。重装/数据清理后所有历史 attachment 卡片无 remoteUrl → broken_image。其实修了 🔴 #1 后,`attachmentLocalPath.split('/inbox/').last` 形如 `<uploadId>.<ext>` 已经天然含 uploadId,只要从那里 split 就能 fallback。 | 改写 `_requestImageDownloadUrl`:优先 ImageRefStore;失败时 fallback 取 `attachmentLocalPath.split('/inbox/').last.split('.').first`(`<uploadId>.<ext>` 形式)作为 uploadId 直接发请求。 |
| 8 | 🟡 警告 | `MacClient/.../Services/SlashCommandBridge.swift:96-113` | **plugin 目录多 version 时遍历所有,可能 produce 重复 source 或较旧版本污染**。代码注释"取目录列表第一个,通常只一个"但实际 `for v in versions` 遍历了**所有**。被 `seen.insert(name)` 去重,但若新旧版本里命令同名,留下的是 contentsOfDirectory 首遇的(可能是旧版),source 标签仍按 plugin 目录名,**用户看到的 description 可能来自旧版**。 | 按 `mtime` 取最新一个 version(`url.resourceValues(forKeys: [.contentModificationDateKey])`),只扫该 version 下的 commands。 |
| 9 | 🟡 警告 | `Server/internal/image/image.go:289-302, 308-340` + 第二轮 #13 | **Server 重启后内存 pendingUpload 全失,但 inbox 文件仍在 → fetched 文件永久泄漏 + phone 端历史图全 broken_image**。`pending` 是纯内存 map(代码注释"Persistence is intentional"),Server 重启:(a) GC 不知道 inbox 目录里的旧文件 → 永久残留;(b) phone 后续发 `image.download.url(upload_id)` 都返回空 URL,**所有 phone 端历史 chat attachment 卡片回退到 broken_image**;(c) 文档承诺的 7 天 TTL 实际是"server 不重启的 7 天"。 | (a) inbox 目录文件名 = upload_id,server 启动时扫一遍目录重建 pending(`Fetched=true,FetchedAt=mtime`);(b) 把 pending 状态持久化到 sqlite,与现有 device 表共用 db。 |
| 10 | 🟡 警告 | `MacClient/.../Services/WSClient.swift:46, 309-315` + 第一/二轮 #10/#16 | **`intentionalCancel` 单标志位无法 cover 重叠 cancel 场景**。用户在 Preferences 快速改 Server 地址 N 次(0.5s 内 A→B→C),task A/B 都被主动 cancel,但 didCloseWith 是异步入队;A 的 close 进来消费 intentionalCancel=false,B 的 close 看到 false → 触发 handleDisconnect → reconnect 风暴。 | 改 `Set<URLSessionWebSocketTask>` 记录所有被主动 cancel 的 task;或 task ===self.task 比对(receive callback 已实现,didCloseWith 应同步加同样判断)。 |
| 11 | 🟡 警告 | `MacClient/.../Services/InputInjector.swift:74-82` + 第一/二轮 #9/#17 | **图片注入 200ms 经验时序在低端 / 高负载 Mac 场景失败 + slash command 注入路径无害 + 长文件名注入鲁棒性未保证**。修了 🔴 #1 后,inject path 是 `inbox/<uploadId>.<ext>`,uploadId 是 UUID,从根上避免了空格/特殊字符问题,这点是好的。但 200ms 仍是经验值,Mac 系统繁忙时可能不够。 | 缓解:把 200ms 拉到 400ms;长期方案:监听 PTY 输出含 file pill 标识(`📎`)再发回车,但读 PTY 增加复杂度,建议 400ms 即可。 |
| 12 | 🟡 警告 | `AndroidClient/.../features/chat/widgets/ask_user_question_card.dart:23-27` + 第二轮 #6 | **AskUserQuestionCard `_sent` 是 widget 私有 state,ListView 滚出视口后 widget dispose,_sent 状态丢失,用户可重复点击 + 重复发送回答**。即便有 `key: ValueKey(m.uuid)`,Flutter 在 element off-screen 时可能 dispose StatefulElement。一旦用户连点多次,会触发 N 条 user 回答污染 Claude 上下文。 | _sent 状态提升到 Message model(message.dart 加 `askUserAnsweredAt` 字段,chat_repository 维护)或在 chat_repository 维护内存 set。 |
| 13 | 🟡 警告 | `AndroidClient/.../features/chat/widgets/attachment_card.dart:108` + 第二轮 #8 | **`File(localPath).existsSync()` 在 build() 内同步 IO,每次 ListView 滚动重建都触发**。cacheExtent=600 内 N 个 attachment_card 每次 build 都做磁盘 stat。慢闪存上滚动卡。更糟:JSONL 解析的 attachment localPath 是 Mac 绝对路径(`/Users/.../inbox/...`),Phone 端 existsSync 永远返回 false 但每次都做无用 syscall。 | (a) 用 cached bool(message 上加 `localExists` 字段,在 _mergeMessages 时一次性 stat);(b) Mac 端注入的 attachment localPath 不当本地路径(在 message.dart `_rewriteInboxRefAsAttachment` 时不设 localPath,或加 `isRemoteOriginPath: true` flag)。 |
| 14 | 🟡 警告 | `AndroidClient/.../features/chat/widgets/assistant_text_card.dart:117-145` + 第二轮 #9 | **HighlightView 每帧重建无缓存,与需求 24 滑动优化方向相反**。flutter_highlight 同步 tokenize,长 markdown 答复含数百行代码时,scroll 到该 card 帧率下掉。 | (a) `RepaintBoundary` 包裹 HighlightView 减少重绘;(b) 给 message uuid 的 code 块做内存 cache;(c) >200 行 fenced block 退回普通 monospace。 |
| 15 | 🟡 警告 | `AndroidClient/.../data/chat_repository.dart:232-244` + 第二轮 #25 | **sendTextWithImages 在所有图上传失败时仍发 text,语义错位**。line 268-270 `if (text.isNotEmpty) { await sendText(tabId, text); }` 没判"至少一张图上传成功"。用户场景:5 图全 fail + text "看下这几张图" → 文字仍送达 Claude(Claude 不知道有附件 → 无法理解)。 | 至少一张图成功才发 text;全失败时弹 SnackBar"上传全部失败,文字未发送,请重试"。 |
| 16 | 🟡 警告 | `AndroidClient/.../features/chat/widgets/input_bar.dart:135-170` + 第二轮 #26 | **`_send` 期间 InputBar 整体未 disable,用户可在长 5 张图上传期间触发第二次 sendTextWithImages,导致多个上传任务并发跑**。setState 后 `_busy=true` 但 widget.enabled 没改;加号/slash button 仍 onPressed 有效;TextField 仍可编辑。 | `_busy` 也参与 disable 判断:加号/slash/TextField 的 onPressed/enabled = `widget.enabled && !_busy`。 |
| 17 | 🟡 警告 | `AndroidClient/.../features/chat/widgets/input_bar.dart:80-89, 119-133` + 第二轮 #27 | **超过 5 张图选择 / 文件 >20MB 静默丢弃,无用户提示**。`_pickGallery` 多选时只保留前 5 张(`if (_pending.length >= 5) break;`),没提示;`_pickFile` 选 >20MB 文件直接落到 _pending,上传时才抛错。 | 选择时如果丢弃发 SnackBar:"最多 5 张,已忽略 N 张";文件 pick 时预 read file.length 与 20MB 比对,超出立即提示不入 _pending。 |
| 18 | 🟡 警告 | `AndroidClient/.../features/chat/widgets/input_bar.dart:191-196` + 第二轮 #7 | **快速连击删 thumb 可能 RangeError**。`itemBuilder: (_, i) => _Thumb(... onRemove: () => setState(() => _pending.removeAt(i)))`。i 是闭包绑定,极速连击两个 thumb(0.1s 内),第二次 callback 用旧 i,可能 removeAt 越界。 | 改按 file 引用移除:`_pending.removeWhere((f) => identical(f, file))`。 |
| 19 | 🟡 警告 | `Server/internal/server/server.go:391-405` + 第二轮 #12 | **`image.download.url` 无 phone 权限校验,跨 phone 横向访问其它 phone 上传的 upload_id**。`RequestDownloadURL` 只查 pending[uploadID] 不验证调用 phone 是否当初 uploader。在"多 phone 共享同 Mac"个人工具场景下属 by-design(协作看同一 chat),但**未文档化也无登记机制**。一旦半信任 phone 在线,只要知道 upload_id(JSONL `@<path>` 文本里完全暴露)就能拉所有图。 | (a) 文档 docs/ 写"多 phone 全互信" 设计声明;(b) image_upload_begin 时记 phone tokenHash,download.url 校验调用方 tokenHash == uploader tokenHash;(c) 已 fetched 放宽,unfetched 严格 owner-only。 |
| 20 | 🟡 警告 | `Server/internal/server/server.go:240-253` + 第一/二轮 #13/#14 | **新 phone bind 触发 tab.list 广播给所有 phone(N² 流量)**。phone B 一上线,Mac 收到 tab.list.request 回 tab.list.response,router `RouteFromMac` 广播给**所有 phone**(line 47 `protocol.TypeTabListResponse` 在 RouteFromMac 走 broadcast)。N 个 phone 在线时,新 phone 上线 → N 次刷新。叠加需求 9 自动重连,网络抖动重连时每次全局刷。 | router 加 `TypeTabListResponse` 特例:envelope 带 `target_phone` 字段,单播该 phone;或者 server 不主动 push request,只在 phone 主动 fetch 时定向回。 |
| 21 | 🟡 警告 | `Server/internal/server/server.go:200-209` + `MacClient/.../InputInjector.swift:52-78` + 第一/二轮 #14/#23 | **Mac 离线 >5min 后上线收到 input.image,URL 已过期 → 下载失败 + 无重签兜底**。server `pu.ExpiresAt` 用 uploadTTL(5min),`input.image` 推送签的 token 也是 5min。Mac 重连成功后从 server 投递这条消息,5min 后下载 401 → reportInjectError → phone 端只看到 input.error,不知原因。 | (a) Mac InputInjector 收到 input.image 时先试下载,失败时主动调 `image.download.url` 重签后重试;(b) server 推送 input.image 时若 Mac 离线,延迟到 Mac 上线时重签 token 再发。 |
| 22 | 🟡 警告 | `Server/internal/protocol/messages.go:58-65` | **协议常量分组对齐微瑕疵 + 缩进不一致**。新加的 `TypeImageDownloadRequest` / `TypeImageDownloadResp` / `TypeSlashListRequest` / `TypeSlashListResponse` 与 `TypeImageUploadExpired` 在同一 const 块中混排,但缩进/对齐略乱(`TypeImageUploadExpired   = ...` vs `TypeImageDownloadRequest = ...`)。`go fmt` 会自动对齐,本地是否跑过 fmt 不确定。 | 跑 `gofmt -w Server/...` 统一格式化(细节不影响功能,但 review 时易看错)。 |
| 23 | 🟡 警告 | `AndroidClient/.../data/chat_repository.dart:467-491` + 第一/二轮 #6/#21 | **attachment dedup 同名重复发送场景下命中错位**。修了 🔴 #1 后 unifiedFilename = `<uploadId>.<ext>`,理论上不再同名,但有 race window — 上传完成前 attachment.filename 还是原名 `IMG_001.jpg`,此时若 server attachment(filename=`<uploadId>.jpg`)先到 → dedup 用 server 形式 vs 本地未升级形式,匹配不上 → 出现两张 attachment。 | dedup 命中后立即把该 local 卡片 push 到 consumed set,后续匹配跳过;或 dedup 也尝试匹配 `<uploadId>.<ext>` 形式与 inbox path 末段。 |
| 24 | 🟡 警告 | `AndroidClient/.../models/message.dart:218-221` + 第二轮 #29 | **元数据兜底 `if (topType != null && raw['message'] == null && raw['sessionId'] != null) return const [];` 偏宽容,无 warn log 难追溯**。Claude JSONL schema 演进时,新的元数据类型可能漏过 `metaTypes` set,兜底分支会静默吃掉。 | 走兜底分支时 `print('[message.dart] meta fallthrough: topType=$topType keys=${raw.keys.toList()}')` 或 logger.debug,便于将来追溯。 |
| 25 | 🟡 警告 | `AndroidClient/lib/data/chat_repository.dart:30, 296-309`、`models/message.dart:165-167` | **没看到有任何已 review 的修改文件的单元测试覆盖**。整个本次修改涉及的关键逻辑:dedup(text/attachment)、`_rewriteInboxRefAsAttachment` regex、`/clear` 行为、typing timer、Mac NSString.pathExtension 与 Phone substring 等价性 — 均无 unit test 验证。后续 schema 演进 / regex 微调 / 边界值变化时,容易回归。 | 至少给 message.dart `fromRaw` 各分支(meta type、isMeta、command-name、嵌套 list/string content、空白 text)、chat_repository `_consumeMatchingPendingText`、`_rewriteInboxRefAsAttachment` 5 类典型场景写 widget/unit test;Mac 端 `(p.filename as NSString).pathExtension` 与 dotfile/无 ext/多 dot 文件名的等价性写 Swift XCTest。 |
| 26 | 🟡 警告 | `MacClient/.../Services/InputInjector.swift:74` + 多端协议 | **InputInjector 直接写 `processHost?.write(to:string: "@\(localURL.path)\r")`,path 注入到 PTY 完全未 sanitize**。修了 🔴 #1 后 inbox 文件名 = `<uploadId>.<ext>`(全 UUID + 数字 + ASCII 扩展),根上规避了 shell 元字符问题。但 inboxDir 本身来自 `PreferencesService.appSupportDir`,如果用户改 home 目录到含空格/特殊字符的路径(`/Users/张 三/...`),整条 path 注入到 PTY 仍可能被 Claude TUI 解析失败。 | 注入前对 path 整体做 shell-quote(若 path 含空格则加双引号),或文档限制 user home 不含空格;当前文件名安全已经 cover 大多数场景,此项属低优先级。 |
| 27 | 🟡 警告 | `MacClient/.../Services/WSClient.swift:142-159` | **心跳 timer 用 `Timer.scheduledTimer` 默认在 RunLoop.main / commonMode 上,Mac 系统休眠或长时间无 user 交互时可能延后触发**。Mac 笔记本盖盖儿后 RunLoop 会暂停,timer 不触发,但 socket 还活着;唤醒后可能突然 fire 多次 catch-up,影响 pongMissedCount 统计。 | 用 `DispatchSourceTimer` 替代 Timer.scheduledTimer,或在 NSWorkspace willSleep/didWake 通知里手动 stop/restart 心跳。低优先级,Mac 笔记本休眠场景较少触发。 |

### 建议级(🟢)

| 序号 | 文件:行号 | 描述 | 建议 |
|-----|----------|------|-----|
| 28 | `AndroidClient/.../data/image_ref_store.dart:43-49` | put 每次写整 map JSON,频繁发图时 IO 量多;长期使用累积 SharedPreferences entry 没有 size 上限,500+ 条时启动延迟可感。 | LRU 限制 200 条;或拆 key-per-entry 写入。 |
| 29 | `AndroidClient/.../models/message.dart:392-419` | `_rewriteInboxRefAsAttachment` regex 写死 `cc-anywhere/inbox/`,Mac 端 inboxDir 路径里这个段是约定字符串。若 Mac 端 PreferencesService.appSupportDir 命名调整(从 cc-anywhere 改名),regex 就失配。 | 把 `cc-anywhere/inbox/` 抽出常量,Mac 端 PreferencesService 同样用该常量,降低跨端硬编码 drift 风险。 |
| 30 | `AndroidClient/.../features/chat/widgets/message_card_list.dart:124-152` + 第二轮 #18 | assistantTyping 在 loadingMore=true && messages 为空时一并显示 → 视觉"先思考再 loading"。 | typing 仅在 `!loadingMore && renderMeta.isNotEmpty` 时显示。 |
| 31 | `MacClient/.../Services/SlashCommandBridge.swift:133-151` | yaml frontmatter 简单解析只 strip `"`,不处理 `'` 或多行字符串。Claude 官方文档建议 plugin 作者用单引号或多行写 description,容错性偏弱。 | 引入轻量 yaml parser 或至少 strip 单引号 + 行内 `<...>` 单行 description 通用 case。 |
| 32 | `AndroidClient/lib/data/chat_repository.dart:14-50` `TabChatState.copyWith` | `lastError` 参数没用 `?? this.lastError` fallback(其它字段都用了)。若调用方不传 lastError,新 state 的 lastError 恒为 null。可能是有意设计(每次都清错误),但与其它字段写法不一致,易困惑。 | 加注释明确"lastError 不沿用,每次显式传";或对齐 `?? this.lastError` 行为并增加 `clearError` 显式入口。 |
| 33 | `AndroidClient/lib/data/chat_repository.dart:113-154` `sendText` | `/clear` 命中后没有 short-circuit:仍走"本地 echo + send + _replaceByUuid"完整链路,本地 echo 一条 user text 卡片"/clear" 被加到 list(`_clearTabMessages` 已清了 messages,但 `_mergeOne` 又 add 进来),用户在 list 顶部能看到一条 "/clear" 卡片。考虑到 `<command-name>/clear</command-name>` JSONL 行被过滤,不会有 server uuid 升级,这张 "/clear" 卡片永远以 local-uuid 留在列表。 | `/clear` 命中后直接 send + 不挂本地 echo + 不 _setTyping(true);或 _clearTabMessages 后才挂 echo,效果一致但语义更清。 |
| 34 | `MacClient/.../Services/InputInjector.swift:74-82` | 注入两次回车间 `try? await Task.sleep(nanoseconds: 200_000_000)`,200ms 是裸数字,意图(等 file pill 解析)写在了注释里。 | 抽出常量:`private static let filePillSettleDelay: UInt64 = 200_000_000  // 200ms — see comment` 便于后期统一调优。 |
| 35 | `Server/internal/image/image.go:200-209` | `InputImage` 推送时签 token 用 5min `pu.ExpiresAt`,与 fetched 后改用 fetchedTTL(7天)的设计意图不一致(注释 line 257-260 暗示 fetched 后是 7 天)。 | 推送 input.image 时已用 uploadTTL,但 Mac 立即 fetch 一次后续 phone 拉用 fetchedTTL,两段语义清晰。建议在代码注释里写明这两条时间线避免误解。 |
| 36 | `Server/internal/router/router.go:42-58` | `RouteFromMac` 用 switch case 列举 type,新加 type 时易漏(例如 `TypeSlashListResponse` 这次加了 — 如果忘加, slash command response 永远不会送到 phone)。 | 添加单元测试 case-by-case 覆盖每个 type;或 router 维护一个白名单 set + 自动注册机制。 |
| 37 | `AndroidClient/.../data/chat_repository.dart:285-330` + `image_ref_store.dart` | `_imageRefStore?.put` 是 fire-and-forget(没 await 失败处理),如果 SharedPreferences 写失败(磁盘满/权限),会静默丢失映射,后续历史图预览全坏。 | put 失败时 logger.warn,且把 in-memory `_map[filename] = uploadId` 落地(目前已经这么做,持久化失败只丢磁盘版,在线状态下功能仍正常)— 这点其实已 OK,只是没有显式 warn log。 |
| 38 | 多文件:`AndroidClient/.../chat_repository.dart` 中的 logger 调用 | 所有 log 都走 `_log.warn('Chat', '...')`,没有结构化字段(tabId / uploadId / msgUuid)。调试时只能 grep 文本,定位困难。 | logger 改成 `_log.warn('Chat', 'image upload failed', extra: {'tabId': tabId, 'uploadId': uploadId, 'error': e})` 结构化(若 AppLogger 不支持 extra,加一个 overloaded 签名)。 |

---

## 代码质量评分

| 维度 | 评分 | 说明 |
|------|-----|-----|
| **命名 / 可读性** | 4/5 | 命名规范一致(snake_case 在协议层、camelCase 在 Swift/Dart);关键复杂逻辑(dedup/正则)有 WHY 注释解释设计意图;中文 + 英文混排在不同文件中有约定(注释多为中文,API doc 多为英文)。扣分点:`_recordLocalPendingAttachment` / `_consumeMatchingPendingAttachment` dead code 未清,降低可读性。 |
| **错误处理** | 3/5 | try/catch 覆盖广,但部分错误静默吞(image.download.url.response 空 URL → 直接 return 卡死 UI;ImageRefStore.put 失败无 warn;`reportInjectError` 通知 phone 但 phone 不显示原因)。用户视角错误信息不够明确(broken_image 看不到原因)。需求 5 sendText 失败已有 sendFailed 标记 + 用户重试入口,做得好。 |
| **日志记录** | 3/5 | 关键路径(连接重连/心跳/upload/inject)都有日志,且 log 级别合理(error/warn/info/debug 分得清);但缺结构化字段(tabId/uploadId 等),无敏感数据泄露问题(token 不写 log)。WSClient debug 级别"receive callback ignored (stale task)"有助于排查重连风暴问题,值得点赞。 |
| **安全性** | 3/5 | HMAC URL 签名设计合理;但 HttpOverrides.global 全局 trustAll 与 Settings 开关语义割裂(🟡 #1);三端 trust 策略碎片化(🟡 #2);image.download.url 跨 phone 无权限校验(🟡 #19),个人工具场景可接受但需文档化。InputInjector 注入 PTY 的 path 在修了 🔴 #1 后已通过 UUID 命名规避大部分注入风险。 |
| **性能** | 3/5 | 需求 24 message_card_list lazy 渲染 + cacheExtent=600 是好的;但 history reload 时 N 张图 N 次 download.url 请求(🟡 #6);attachment_card existsSync 同步 IO(🟡 #13);HighlightView 无 cache 每帧 tokenize(🟡 #14)。三个 N×性能问题未修。 |
| **资源管理** | 3/5 | dispose 已 cancel typingTimers(第二轮复审已修);但 `_pendingDownloadUrl` 在 /clear 时未清(🟡 #4);`_localPendingAttachments` 持续 append 无消费(🟡 #3);Server pending 内存 map 重启全失(🟡 #9)。WSClient task/session 关闭路径完整。 |

**总分:19/30**

---

## 三端协议风格一致性

| 维度 | 评估 | 结论 |
|------|-----|-----|
| **字段命名** | 协议层统一 snake_case(`tab_id` / `upload_id` / `image_url`);Mac Swift / Android Dart 内部用 camelCase,在 JSON 编解码层桥接;Go struct 用 PascalCase + json tag snake_case。 | ✅ 一致 |
| **type 字符串值** | `slash.list.request` / `image.download.url` / `image.download.url.response` 三端 100% 对齐(已在第一轮报告核对)。 | ✅ 一致 |
| **常量复用** | Go 用 `protocol.TypeXxx` 常量;Mac Swift 部分使用字面量(`"slash.list.request"`),Phone 用 `ProtocolType.slashListRequest` 常量。 | 🟡 不一致 — Mac Swift 应抽取 `ProtocolType` 常量层,与 Phone 对称 |
| **错误码** | Go `CodeXxx` / Mac `ErrorPayload.code` / Phone `ProtocolErrorCode` 三端字符串值一致。 | ✅ 一致 |
| **注释语言** | 三端代码内中文注释为主,API doc/方法签名以英文为主;Server Go 注释偏英文,Phone/Mac 偏中文。 | ⚠️ 不严格一致但可接受 — 个人工具场景下没问题 |

---

## 回滚方案评估

| 场景 | 兼容性 | 说明 |
|------|-------|-----|
| 新版客户端 + 旧 Server | ✅ 兼容 | phone 发 `image.download.url` / `slash.list.request` 时,旧 Server router 不在白名单,会 `slog.Debug("unroutable from phone")` 静默丢弃。phone 端永远等不到 response,UI 显示 broken_image / 空 slash sheet。降级 OK 但用户体验差;建议新版 phone 在加载新协议前先做 server 版本探测。 |
| 旧版客户端 + 新 Server | ✅ 兼容 | 旧 Mac/phone 不感知 `image.download.url` / `slash.list.*`,不会发送也不会收到;Server 收到旧版 phone 的旧协议(`image.upload.begin` 等)行为不变。 |
| 旧版 phone secure_storage 升级到新版 | ✅ 兼容 | ServerConfig.fromJson `trust_self_signed` 缺失时 fallback `?? true`,默认信任。 |
| Server 重启 | ❌ **不兼容**(🟡 #9) | pending 内存全失,所有历史 attachment 卡片回退 broken_image;inbox 文件残留磁盘 — 这是上线前需要解决的回滚级问题。 |
| Mac 重启 | ✅ 兼容 | tabs.json 本地持久,`claude -c` 恢复对话;ImageDownloader.inboxDir 旧文件由 purgeOldFiles(7 天 mtime)清理。 |
| Phone 重装 | 🟡 部分兼容(🟡 #7) | ImageRefStore 丢失 → 历史图全 broken。当前缺 fallback;修了 🟡 #7 后 OK。 |

**回滚方案结论:** Server 重启场景(🟡 #9)和 Phone 重装场景(🟡 #7)是上线前应优先解决的兼容性问题。其余场景兼容良好。

---

## 测试覆盖

经过本次审查范围内文件搜索,**未发现单元测试或集成测试**覆盖以下关键逻辑:

| 关键逻辑 | 文件 | 当前覆盖 | 风险 |
|---------|------|---------|-----|
| message.dart `fromRaw` 多分支(meta type / isMeta / `<command-name>` / 嵌套 list/string) | `models/message.dart` | 无 | 🟡 schema 演进时易回归(本次零散需求里就修了 5 次解析逻辑) |
| `_rewriteInboxRefAsAttachment` regex | `models/message.dart:392-419` | 无 | 🟡 path 边界 case(空格/dotfile/含 `cc-anywhere/inbox/` 但末段无 ext)易漏 |
| dedup(text + attachment) | `data/chat_repository.dart:467-491` | 无 | 🔴 业务关键 — 修了 🔴 #1 后只在三轮 review 推演,无 unit test 锁定 |
| Mac NSString.pathExtension 与 Phone substring 等价性 | InputInjector.swift:61-66 + chat_repository.dart:225-226 | 无 | 🟡 dotfile case 复审时刚发现 |
| typing timer 60s 兜底 | `data/chat_repository.dart:578-591` | 无 | 🟡 容易在重构时漏掉 cancel 路径 |
| WSClient intentionalCancel + stale task | `Services/WSClient.swift:309-315` | 无 | 🟡 重连风暴是上线最易触发的故障类 |

**建议在 Review-3 之后零散需求清理时,补 5-8 个核心 unit test 覆盖以上关键链路。**

---

## 审查结论

- [ ] 通过(无问题)
- [x] **通过(可上线但留 N 个 🟡 警告)**
- [ ] 不通过

### 结论说明

**本轮未发现 🔴 阻塞问题**。所有第二轮报告中的 🔴 修复均已生效(见上文"已确认修复"表)。

发现 **27 个 🟡 警告 + 11 个 🟢 建议**,其中:

1. **第二轮复审报告标"必修"的 5 个遗留 🟡 中,本轮发现 2 个仍未修复**:
   - 🟡 #3 dead code `_recordLocalPendingAttachment`
   - 🟡 #4 `_clearTabMessages` 未清 `_pendingDownloadUrl`

2. **本轮新发现的 🟡 警告**:
   - 🟡 #22 protocol/messages.go 缩进格式(细节但可读性差)
   - 🟡 #25 测试覆盖整体缺失(系统性问题)
   - 🟡 #27 WSClient Timer 在 Mac 休眠场景的细节

3. **第二轮报告中未修的 🟡 中,影响上线/回滚的优先**:
   - 🟡 #9 Server 重启 pending 全失 → 所有历史图 broken_image(回滚硬伤)
   - 🟡 #7 ImageRefStore 重装/丢失 → 历史图全黑(重装回归)
   - 🟡 #6 history reload 触发 50× download.url 请求(性能洪流)
   - 🟡 #12 AskUserQuestionCard `_sent` 滚出视口丢失(数据完整性)

### 上线建议

**可以发布**,但建议 Review-3 之后立即处理以下 6 项以达到生产就绪标准(均不需要大改):

1. 🟡 #3 / #4 — 清 dead code + 修 `_clearTabMessages`(15 分钟修完)
2. 🟡 #5 — image.download.url.response 空 URL 时给用户错误提示(20 分钟)
3. 🟡 #7 — ImageRefStore fallback 取 `attachmentLocalPath` 末段 uploadId(10 分钟)
4. 🟡 #9 — Server 启动扫 inbox 目录重建 pending(40 分钟)
5. 🟡 #16 / #17 — input_bar `_busy` 期间 disable + 选图超量/超大文件提示(30 分钟)

合计 ~2 人小时,即可清零所有"上线即用户能立刻撞上"的 🟡 警告。

剩余 🟡 / 🟢 可纳入下次零散需求迭代清理。

### 总体评价

**代码质量良好,主流程严谨,可上线**。第一/二轮发现的所有 🔴 阻塞和高优先 🟡 警告(/clear typing 卡住、typing 无兜底、同名图三处覆盖、dotfile 不一致)全部修复且修复有效。但仍有少量第二轮复审已标"必修"的 🟡 警告本轮发现没有落地,以及一些系统性问题(测试覆盖、HttpOverrides 语义割裂、Server 重启状态全失),需要在下次零散需求迭代或正式发布前清理。

