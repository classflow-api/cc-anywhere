# 第二轮代码审查报告 - 边界值与业务影响

## 审查信息

- 审查轮次：第二轮 / 共三轮
- 审查日期：2026-05-13
- 审查范围：Server + MacClient + AndroidClient 全代码库
- 审查重点：边界值、集合边界、并发场景、协议兼容性、权限控制、资源管理、网络异常

## 审查范围

| 端 | 目录 | 实际审查文件数 |
|----|------|--------------|
| Server (Go) | `/Users/liangliyu/project/cc-anywhere/Server/` | 13 个 `.go` + 1 个 `.sql` + Dockerfile + yaml |
| MacClient (Swift) | `/Users/liangliyu/project/cc-anywhere/MacClient/Sources/CCAnywhere/` | 43 个 `.swift` |
| AndroidClient (Flutter/Dart) | `/Users/liangliyu/project/cc-anywhere/AndroidClient/lib/` | 41 个 `.dart` |

---

## 边界场景核查表

### 1. 字符串与文件名边界

| 场景 | 实现位置 | 状态 |
|------|---------|------|
| Tab name 空字符串回退文件夹名 | `TabManager.swift:50-52` | ✓ |
| Tab name >50 字符抛 nameTooLong | `TabManager.swift:52-53` | ✓ |
| 文件夹路径含空格 | `Tab.swift:51` `URL(fileURLWithPath:)` | ✓ |
| 文件夹路径含中文 / Emoji | URL 标准化 + `standardizedFileURL` | ⚠ JSONL 编码路径 `/`→`-` 对 emoji 路径未做 case fold（小概率冲突） |
| 设备名超 30 字符 | Android `device_name_screen.dart:60` substring(0,30) | ✓ |
| 设备名空字符串 | `device_name_screen.dart:50-53` 拒绝 | ✓ |
| sub_token 长度 < 4 在 settings 显示 | `settings_screen.dart:41` `clamp(0, length-4)` | ✗ 见问题 #1（短 sub_token 触发 RangeError） |
| text 长度 4000 字符上限 | Android `input_bar.dart:137` maxLength | ✓ Mac 端 InputInjector 无对应限制 |
| text 含 `\r` `\n` 已存在被注入 PTY | `InputInjector.swift:43` 直接拼 `\r` | ⚠ Claude Code 可能识别为多次回车 |
| Token 哈希长度异常（非 hex / 空串） | Server `auth.go:HashToken` 永远输出 64 字符 hex | ✓ |
| Server bind token 为空字符串 | `auth.go:43-55` VerifyMaster 用 constant-time 比较 | ✓ (永远 mismatch) |

### 2. 数值边界

| 场景 | 实现位置 | 状态 |
|------|---------|------|
| 图片 size = 0 | Server `image.go:94` `req.Size <= 0` 拒绝 ImageTooLarge | ✓ |
| 图片 size = 20 MiB | `image.go:94` `> maxImageBytes` 才拒绝 | ✓ (= 20 MiB 通过) |
| 图片 size > 20 MiB | `image.go:94 + 152 + 167-179` 三层拦截（声明大小、Content-Length、LimitReader+1 兜底） | ✓ |
| msg.history.request `limit` = 0 / 负数 | `HistoryBridge.swift:45` `min(max(1, req.limit ?? 50), 200)` | ✓ |
| msg.history.request `limit` 超过 maxLimit | `HistoryBridge.swift:45` cap 200 | ✓ |
| port 范围 1-65535 | Mac `ServerConfig.swift:31`、Android 仅 `as int` 无校验 | ⚠ Android 未校验 port 范围 |
| pendingTTL = 5 分钟 | Server `device.go:22` | ✓ |
| 重连退避 [1,3,10,30] | Mac `WSClient.swift:42`、Android `ws_client.dart:308` | ✓ (持续 30s 不停止) |
| 接收 4 MiB WS 消息上限 | Server `connection.go:31-52` `readSizeLimit=4 << 20` | ⚠ 见问题 #2（大 JSONL 行会切断） |

### 3. 集合 / 文件 IO 边界

| 场景 | 实现位置 | 状态 |
|------|---------|------|
| 启动时无 `tabs.json` | `TabManager.swift:114-125` 静默忽略 | ✓ |
| `tabs.json` 损坏（非 JSON） | `loadFromDisk:121-123` 清空 + log.error | ✓ |
| 单 Tab / 多 Tab / 0 Tab 状态切换 | TabManager + TabStripView | ✓ |
| 启动时 Mac 端无 server-config.json | `PreferencesService.swift:69-76` 用默认空 ServerConfig | ✓ |
| JSONL 文件 0 字节 | `JSONLWatcher.swift:208-220` `data.isEmpty` 返回 | ✓ |
| JSONL 文件 1 行 / 不完整行 | `JSONLWatcher.swift:222-224` `omittingEmptySubsequences:true` | ✓ 但最后一行未带 `\n` 时会被丢弃直到下次 FSEvent 重新读取（注释自承） |
| JSONL 文件 > 1 MiB | `JSONLWatcher.swift` 全量读 chunk → 内存膨胀；history `Data(contentsOf:)` 一次读全 | ⚠ 见问题 #3 |
| JSONL 在监听过程中被 rotate（session 切换） | `JSONLWatcher.swift:177-205` 每次 FSEvent 都 re-identify | ⚠ 见问题 #4（rotate 时 lastOffset reset 但同 session 内连续追加可能重复 emit） |
| `~/.claude/projects/<encoded>` 目录还不存在 | `JSONLWatcher.swift:117-124` 监听父目录 | ✓ |
| 空设备列表 | `DeviceManager` / Android `tab_repository` 默认空集合 | ✓ |
| 空消息历史 | `HistoryBridge.swift:78-80` 空 list + hasMore=false | ✓ |
| Tab 列表超过 50/100 个 | 无显式 cap；UI 横向滚动；msg.history 每文件读全量 | ⚠ 见问题 #3 |

### 4. 并发场景

| 场景 | 实现位置 | 状态 |
|------|---------|------|
| 同 token 多客户端连接 → 踢旧连接 | Server `hub.go:26-54` AcceptMac/AcceptPhone | ✓ |
| 多手机同时发 input.text | `InputInjector` @MainActor 串行写 PTY；`view.process.send` 单次写入是 syscall 原子 | ✓ |
| Mac 重连同时被踢的竞态 | `hub.go:RemoveConnection:60-74` 判断 `existing == conn` | ✓ |
| Server hub `phoneConns` 并发 | `sync.RWMutex` 包裹 | ✓ |
| WSClient.task 重连时被替换 | Mac `WSClient.swift:59-60` `task?.cancel(); task=nil` 后再赋值 | ⚠ 见问题 #5 |
| TabManager.persist() 与 JSONLWatcher 监听同目录 | TabManager 写 `tabs.json` 到 `~/Library/Application Support/cc-anywhere/`，watcher 监听 `~/.claude/projects/` | ✓ (不同目录) |
| Server image.fetched 与 GC 并发 | `image.go:255-261` HandleFetched + `286-305` sweepExpired，都用同 `s.mu` | ✓ |
| Server inbox 文件被并发上传同 upload_id | `image.go:144` 检查 `pu.Received` 拒绝重复 | ✓ |
| 多客户端共享 sub_token | `hub.go:42-54` 同 hash key → 旧连接被踢 | ✓ |
| PTY EOF / processTerminated 与 InputInjector.write 竞态 | InputInjector.write 检查 `terminalsByTab[tabId] == nil` 返回错误；但 ProcessHost.write 不检查 terminate 状态，可能写入已 terminate 的 view | ⚠ 见问题 #6 |
| Mac MainActor 内部 send Tab 列表与 inbound 处理顺序 | bind.ack → state=.connected → TabSyncBridge.sendFullList()；handleIncoming 是 main actor 同步 → 顺序保证 | ✓ |

### 5. 协议兼容性

| 场景 | 实现位置 | 状态 |
|------|---------|------|
| 缺 `data` 字段 | Server `protocol.Envelope.Data json.RawMessage` omitempty；Mac `data: AnyJSON?`；Dart `data is Map ? .cast : {}` | ✓ |
| 缺 `id` 字段 | Dart `tryFromJson` 校验 `id is String`，否则 return null（拒绝消息）；Mac `Codable` 必填字段，缺则 decode 失败 | ⚠ 见问题 #7（Server 发 id 始终带，但 Mac 对 server 缺 id 的消息会全部丢弃） |
| 缺 `ts` 字段 | Server 始终带；Mac/Android envelope 不含 ts 但能正常解码（field 缺失） | ✓ (运行无影响，仍违反协议 §6.1) |
| `data` 不是对象（string/number/array） | Server 用 `json.RawMessage` 不解码；Mac AnyJSON 接受任意；Dart 强制 `data is Map ?` 失败时降为 `{}` | ⚠ 见问题 #8 |
| 多余字段 | JSON 解码忽略；ok | ✓ |
| sub_token_id 类型 string vs number 兼容 | Mac `asIdString` 容错 string + number；Server 全部用 string | ✓ |
| `force_disconnect` reason 区分 | Mac 一律停止重连；Android `_userDisconnected=true` 才停 | ⚠ 见问题 #9 |
| Server bind.ack 设 `agent_id="default"` | `server.go:195` 写死 | ⚠ 见问题 #10 |

### 6. 数据库变更与边界

| 场景 | 实现位置 | 状态 |
|------|---------|------|
| `master_token` 单行 CHECK (id=1) | `001_init.up.sql:6-11` + `auth.go:62 ON CONFLICT(id) DO UPDATE` | ✓ |
| `sub_tokens` token_hash UNIQUE | `001_init.up.sql:15` | ✓ |
| 状态枚举 `pending/active/revoked` | `CHECK` 约束 + `device.go:17-21` 常量 | ✓ |
| WAL 模式 | `db/sqlite.go:33` `journal_mode(WAL)` | ✓ |
| busy_timeout 5s | `db/sqlite.go:33` | ✓ |
| 索引 status / token_hash | `001_init.up.sql:26-27` | ✓ |
| migrations 幂等（schema_version 表） | `db/sqlite.go:53-108` 跳过已 applied | ✓ |
| migration 失败回滚 | 无 down.sql，无事务包裹（注释自承），出错时无法自动回滚 | ⚠ 见问题 #11 |
| Mac 不发 device.update_name 同步 | Android `settings_screen` 仅展示，不支持改名 | ⚠ (R-A6-03 未实现，前轮已识别) |
| pending → revoked 的累积 | `device.go:96` BindPhone 过期 UPDATE 为 revoked；GC 只 DELETE pending 行，过期 revoked 永久累积 | ⚠ 见问题 #12 |

### 7. 配置与默认值

| 场景 | 实现位置 | 状态 |
|------|---------|------|
| Server 配置文件缺失 | `config.go:47` 直接 `read config` 返回错误 → 启动失败 | ✓ |
| 环境变量未设导致 `${CC_HMAC_SECRET}` 为空 | `config.go:78-80` validate 拦截 | ✓ |
| public_host 缺失（图片 URL 不可生成） | `config.go:69-71` validate 拦截 | ✓ |
| log.level 默认 info | `config.go:81-83` | ✓ |
| Mac trustSelfSigned 默认 false | `ServerConfig.swift:15` | ✓ |
| Android trustSelfSigned 默认 false | `server_config.dart:18` | ✓ |
| Android port 范围未校验 | `server_config.dart:49` `(j['port'] as num).toInt()` | ⚠ 见 #2.5 |

### 8. 权限控制

| 场景 | 实现位置 | 状态 |
|------|---------|------|
| HTTP /upload/{id} HMAC 校验 | `image.go:131-135` verifyToken | ✓ |
| HTTP /download/{id} HMAC 校验 | `image.go:223-226` verifyToken | ✓ |
| HMAC 5 分钟过期 | `image.go:315` `time.Now().After(exp)` | ✓ |
| HMAC 常量时间比较 | `image.go:319` `hmac.Equal` | ✓ |
| WebSocket bind 强制（先发其他不能跳过） | `server.go:166-175` 第一条必须是 bind，否则关闭 | ✓ |
| 60s 未鉴权断开 | `connection.go:30` + `server.go:142` bindTimeout | ✓ |
| device.self_unbind 是否需要 bind 验证 | 必须先 bind 通过才能进 dispatchFromPhone | ✓ |
| /upload/{id} 大小限制（HTTP 上限） | `image.go:152` Content-Length + LimitReader 兜底 | ✓ |
| /download/{id} 需先 Received | `image.go:231` 校验 `!pu.Received` 返回 404 | ✓ |
| /download/{id} 文件路径 traversal 风险 | `pathTail` 截取 `/upload/` 后到下一个 `/` 之前；写入 `filepath.Join(inboxDir, id)` | ⚠ 见问题 #13（upload_id 若含 `..` 或 `/` 早被 pathTail 切掉，但 UUID 来源固定，无注入；建议显式正则） |
| HMAC secret 是否暴露 | `config.go:38` HMACSecret 仅在 image.go 使用，不进 log | ✓ |

### 9. 业务规则边界

| 场景 | 实现位置 | 状态 |
|------|---------|------|
| 同文件夹多 Tab 拦截 (R-M1-04) | `TabManager.swift:41-43` | ✓ |
| 5 分钟 QR 过期 (R-M6-02) | Server `device.go:22` + `device.go:94-98` + GC | ✓ |
| Mac UI 端 QR 5 分钟倒计时 | `DeviceManager.swift:59` 本地计时器 | ⚠ 见问题 #14（用本地时间，与 Server `expires_at` 不联动） |
| 5 分钟 image upload 过期清理 (R-S6-04) | `image.go:271-305` sweepExpired GC（30s 周期） | ✓ |
| tool_use 批准 30s 超时 (R-A5-02) | `tool_use_card.dart:50-60` | ✓ |
| 重连退避持续 30s | Mac/Android 重连永不停止；R-M5-04 描述"持续" | ✓ |
| msg.history `before` 过滤 | `HistoryBridge.swift:107-110` | ✓ |
| msg.history `hasMore` 计算 | `HistoryBridge.swift:114` tailStart > 0 | ✓ |
| 单条消息最多 5 张图片 (R-A4-03) | `input_bar.dart:46` 客户端层 cap | ✓ |
| 图片上传失败 3 次重试 (R-A4-06) | `image_upload_service.dart:37` | ✓ |

### 10. 资源泄漏 / 崩溃路径

| 场景 | 实现位置 | 状态 |
|------|---------|------|
| PTY master fd 在 Tab 关闭时关闭 | `ProcessHost.stopProcess` 调用 `view.terminate()` (SwiftTerm 内部 close PTY) | ✓ |
| App 进程崩溃时遗留 claude 子进程 | `PIDTracker.reapStaleProcesses` 启动时 ps -p comm= 检查 | ✓ |
| WSClient task 在重连前是否 cancel | `WSClient.swift:59-60` cancel + task=nil | ✓ (但 .reconnecting 时未 nil 见 #5) |
| Server 慢消费者断开后是否清理 pending | `connection.go:72-76` 1s send timeout 后强制 Close；out 通道 GC | ✓ |
| Server inbox 文件孤儿（Mac 离线导致永不 fetched） | `image.go:286-305` sweepExpired 5min 兜底 | ✓ |
| Mac inbox 目录无限增长（R-M7-07 7 天清理） | `ImageDownloader.purgeOldFiles` 启动时调用一次 | ⚠ 见问题 #15（仅启动时清理一次，长期运行无效） |
| MacClient 日志 7 天清理 | `Logger.swift:85-98` 启动时一次 | ⚠ 同上（无定时） |
| TabManager 写 tabs.json 失败后内存状态已变更 | `createTab` 先 append 再 persist，persist 抛出但 in-memory `tabs` 已包含新 tab；调用方 try? 忽略 | ⚠ 见问题 #16 |
| FSEventStream 引用计数 | `JSONLWatcher.swift:166-169` Stop + Invalidate + Release | ✓ |
| WebSocketChannel 在 Dart 端 cancelOnError:true 导致重连竞态 | `ws_client.dart:164` `cancelOnError:true` → 错误时 stream 自动取消，`_handleDisconnect` 还在 `_channelSub?.cancel()` | ✓ (重复 cancel 安全) |

### 11. 网络异常

| 场景 | 实现位置 | 状态 |
|------|---------|------|
| DNS 解析失败 | Mac URLSession / Android dio 抛错 → handleDisconnect → 重连 | ✓ |
| TLS 握手失败 | 同上 | ✓ |
| 502 / HTTP 错误 | image upload 通过 dio `validateStatus` 拦截 | ✓ |
| 大消息分片 | Server 4 MiB readLimit，超过断开；Mac/Android send 不分片 | ⚠ 见问题 #2 |
| WS ping 失败检测时机 | Server `connection.go:121-129` ping 失败立即 Close；Mac heartbeat 仅发 ping 不检测 pong | ⚠ 见问题 #17 |
| Mac Server 不可达 5 分钟无 Toast | M8 场景未实现"持续 30 秒 → Toast"（前轮 #22 已识别） | ⚠ 遗留 |
| image upload 网络中断 | dio + retry 3 次 + 重试间隔 1s/2s | ✓ |

---

## 发现的问题

### 🔴 阻塞问题

**无。** 第二轮未发现新的阻塞缺陷。前两轮累计的 11 个阻塞已在前 2 次复审中验证修复。

### 🟡 警告问题（建议修复）

| 序号 | 严重程度 | 文件:行号 | 问题描述 | 修复建议 |
|-----|---------|----------|---------|---------|
| 1 | 🟡 警告 | `AndroidClient/lib/features/settings/settings_screen.dart:39-41` | `cfg.subToken.length.clamp(0, cfg.subToken.length - 4)`：当 `subToken.length < 4` 时 `length - 4` 为负数；Dart `num.clamp` 要求 `lowerLimit <= upperLimit`，否则抛 `RangeError`。空 sub_token 或测试时显式短串会让 SettingsScreen 直接崩溃 | 改为 `cfg!.subToken.length > 4 ? cfg.subToken.substring(cfg.subToken.length - 4) : cfg.subToken`，或复用 `device_name_screen.dart:205-208` 的 `_maskToken` 工具函数 |
| 2 | 🟡 警告 | `Server/internal/server/connection.go:31-52` | `readSizeLimit = 4 << 20` (4 MiB) 包住整条 WS envelope。`msg.stream` 单批次可能包含若干 JSONL 行，每行可能含 `tool_result.content` 大块。如果某行接近或超过 4 MiB（如 Claude 返回大文件读出 / 大 diff），WS 会被 `nhooyr` 切断连接，触发 reconnect。Mac/Android 端也不会预切分。R-S1-04、R-A3-* 隐含期望 history 单批 50 条都能传 | 评估提升 readLimit（如 32 MiB），或在 Mac `MsgStreamBridge` 把超大 line 单独走 `msg.raw` envelope 分次发；或在 history 端按字节数自适应分页 |
| 3 | 🟡 警告 | `MacClient/Sources/CCAnywhere/Services/HistoryBridge.swift:81-84` + `JSONLWatcher.swift:208-220` | `Data(contentsOf: activeFile)` 与 watcher 的 `handle.readToEnd()` 均一次性读全量文件。长会话 JSONL 文件 >10 MiB 很常见。导致：(a) history 单次响应内存峰值与文件大小相同；(b) watcher 启动 identifyActiveSession 时若用户已 chat 很多轮，会全量解析。慢盘 / 大文件场景下主线程阻塞（HistoryBridge 在 nonisolated 但 `Data.init` 同步） | history 改为反向流式读取（`FileHandle` seek 到末尾按 chunk 倒读）；watcher 启动时只做 `seekToEnd` + `offset` 记录，**不必解析全部历史**（历史由 phone 主动 pull） |
| 4 | 🟡 警告 | `MacClient/Sources/CCAnywhere/Services/JSONLWatcher.swift:177-205` | session rotation 处理：每次 FSEvent `identifyActiveSession`，若发现 latest != activeSessionFile 就切换并把 `lastOffset = 0`。在 rotation 边界存在**同一文件被切换-重读全部内容**的可能：例如第一次 event 看到老文件 mtime newer（已读完），第二次 event 新 .jsonl 出现 → 切换并从头读；但旧文件残余增量永远丢失。另：`seenUuids` 在 rotation 时不清空，rotation 跨 session 的同名 uuid（罕见）会被去重当作重复忽略 | (a) 维护多个 active session offset，rotation 时保留对旧文件残余的扫尾；(b) rotation 时清空 `seenUuids` / `seenFallback` 并把旧 session 的 uuid 标记为"已 emit 过"（用 session id 隔离） |
| 5 | 🟡 警告 | `MacClient/Sources/CCAnywhere/Services/WSClient.swift:202-220` `handleDisconnect` | `task` 未置 nil：handleDisconnect 仅设 `state=.reconnecting` 并 schedule connect。此时 `task` 仍指向旧任务实例，业务调用 `send(_:)` 仍能进入 guard let t = task 通过（task 非 nil），随后写入失败 task 报错被 catch。**结果是消息看似 send 成功实则被丢**，调用方拿不到错误反馈 | `handleDisconnect` 中显式 `task?.cancel(); task = nil`；或 send 的 guard 也校验 state ∈ {.connecting, .connected} |
| 6 | 🟡 警告 | `MacClient/Sources/CCAnywhere/Services/ProcessHost.swift:88-101` `write(to:bytes:)` | 写 PTY 时只检查 `terminalsByTab[tabId]` 存在；未检查 view.process.running 状态。若进程在 InputInjector 已通过 nil 校验后、PTY write 前 terminate（onProcessExited 已 fire 但 view.process.send 还在执行），则 send 到已关闭 fd 报 EBADF。当前实现 swallows 错误。这与 R-M7-05 "注入失败必须反向通知手机"不符 | `write` 增加 `view.process.running` 校验；若失败则触发 input.error 反向通知 |
| 7 | 🟡 警告 | `MacClient/Sources/CCAnywhere/Services/WSClient.swift:167-170` + `Models/ProtocolMessage.swift:9-11` | `ProtocolMessage.id` 是非可选 String（must decode）。若 Server 由于历史原因或新版本送来缺 `id` 的 envelope，Mac 端 `JSONDecoder().decode(ProtocolMessage.self)` 会失败 → `log.warn("malformed inbound")` 整条消息丢弃。**Server 当前永远带 id（NewEnvelope 实现）**故功能未坏，但对未来 Server 演进脆弱；同理 ts 字段缺失若 Mac/Android 改成必填会有兼容问题 | `ProtocolMessage.id` 改为 `String?`，仅在路由日志中使用；放宽对未来 envelope 升级的容错 |
| 8 | 🟡 警告 | `AndroidClient/lib/models/protocol_message.dart:28-33` | `tryFromJson` 把非 Map 的 `data` 强转为空 `{}`：`data is Map ? data.cast<String,dynamic>() : <String,dynamic>{}`。但 Server `image.upload.url` 等 payload 总是 Map，并无非 Map 字段；问题主要在 `ping`/`pong`/`presence.mac_online`（无 data 或 data:null）：解码 `null` 不是 Map → data={} → 不影响业务。**但如果 Server 误发 `data: "string"` 这种坏帧**，Android 仍当作正常消息分发到 inbound，可能后续访问 `data['x']` NPE | 显式区分缺失 vs 类型不匹配；非 Map 但非空时 log warn |
| 9 | 🟡 警告 | `MacClient/Sources/CCAnywhere/Services/WSClient.swift:192-195` | 收到 `force_disconnect` 一律 `manualDisconnect=true` 停止重连。当 Server 因升级 / 临时故障下发 force_disconnect 时，Mac 永久离线直到用户手动 reconnect。前一/二轮已识别仍未修复 | 区分 reason：仅 `revoked` / `new_session` 不重连；其它原因仍按指数退避重连 |
| 10 | 🟡 警告 | `Server/internal/server/server.go:195` | `BindAck.AgentID = "default"` 写死。QR payload 也未带 agent_id（前轮 #15 / 复审 #2 / 复审 #2 都识别仍未修复）。功能不阻塞（Android 默认空串），但与协议 §4.1 矛盾 | 生成 / 持久化 agent_id 到 master_token 表（或独立 settings 表），Mac 在 bind.ack 后保存并在 QR payload 中携带 |
| 11 | 🟡 警告 | `Server/internal/db/sqlite.go:95-107` | migrations 执行不在事务内：`conn.ExecContext(ctx, string(sqlBytes))` 直接执行整文件 SQL。多语句 SQL 文件中间报错时前半已 commit，schema_version 未写入 → 下次启动会重复执行已成功语句，可能因 `CREATE TABLE` 已存在而失败（虽然 `IF NOT EXISTS` 兜底）。无 down.sql / 回滚 | 把整个 migration 包在 BEGIN/COMMIT；失败 ROLLBACK |
| 12 | 🟡 警告 | `Server/internal/device/device.go:93-98` + `224-249` | 用户用过期 pending 来 bind 时，过期行 UPDATE 为 `revoked`（line 96）；`StartPendingGC` 仅 DELETE `status='pending'` 行（line 237），过期 revoked 永久累积。前轮 #14、复审 #6 已识别 | BindPhone 过期分支直接 DELETE；或扩展 GC 同时清理 status='revoked' AND created_at 早于阈值的行 |
| 13 | 🟡 警告 | `Server/internal/image/image.go:125-128 + 216-219` | `pathTail("/upload/" + xxx, "/upload/")` 截取到下一个 `/` 前。当前 upload_id 是 server 生成的 UUID，无法注入；但若以后允许客户端传入 ID 或多端组件接入，缺少正则约束（如 `^[a-f0-9-]{36}$`）会留下 path traversal / 任意写入风险 | 增加 `regexp.MatchString("^[a-f0-9-]{36}$", id)` 校验，否则 400 |
| 14 | 🟡 警告 | `MacClient/Sources/CCAnywhere/Services/DeviceManager.swift:59 + 117-128` | QR 倒计时使用本地系统时间 `Date().addingTimeInterval(5*60)`，与 Server 返回的 `expires_at` 不联动。Server 端 5 分钟 GC 是按 server 时钟，若 Mac 时钟漂移（如懒得校时）显示的"剩余时间"与服务端实际不一致：可能用户看到 1 分钟还在显示 QR 但服务端已 GC，扫码后报 TOKEN_EXPIRED；或反过来显示已过期但 server 还能接受 | 解码 `device.subtoken.created` 时取 `expires_at` 字符串 → ISO8601 解析为 Date 直接用 |
| 15 | 🟡 警告 | `MacClient/Sources/CCAnywhere/Services/ImageDownloader.swift:49-62` + `Logger.swift:85-98` | inbox 7 天清理 (R-M7-07) 与日志 7 天清理 (R-M9-02) 都只在启动时执行一次。App 长期运行（一周多不重启）→ 清理失效；崩溃 / 强退也不触发。spec 未明说"每天"但意图显然 | 启动后再起一个 Timer 每 24h 触发一次清理 |
| 16 | 🟡 警告 | `MacClient/Sources/CCAnywhere/Services/TabManager.swift:38-64 + 66-87` | `createTab`/`removeTab`/`renameTab` 都是先 mutate in-memory（append / remove / 改 name）再 `try persist()`。若 persist 抛出（磁盘满 / 权限错），内存仍保留变更而 `tabs.json` 未更新，**应用重启后状态回退**。createTab 内 `persist()` 失败时还会触发 `changes.send(.added)` 通知 Server tab.changed | persist 失败时回滚 in-memory；或采用"写入后再 push 状态"模式：先写盘成功再 send change 事件 |
| 17 | 🟡 警告 | `MacClient/Sources/CCAnywhere/Services/WSClient.swift:114-129` | Mac 端心跳只发 ping，未检测 pong 回程超时。若 Server 端到端阻塞（TCP keepalive 还活着但 application 已死），Mac 不会主动断开，要等到 URLSession 层超时（默认 60+s）。Android `ws_client.dart` 同理只 send ping 不验证 pong | 维护 last_pong_at；每次心跳判断与最近 pong 间隔 > 60s 则主动 disconnect → 触发重连 |

### 🟢 建议优化

| 序号 | 严重程度 | 文件:行号 | 问题描述 | 修复建议 |
|-----|---------|----------|---------|---------|
| 18 | 🟢 建议 | `AndroidClient/lib/data/chat_repository.dart:295-304` | `msg.raw` 处理时 uuid 用 `microsecondsSinceEpoch` 不稳定，重复推会渲染两次（前轮 #24 / 复审 #9 仍未修复） | 改为 `raw-${tabId}-${line.hashCode}` 或 sha256(line) 稳定 key |
| 19 | 🟢 建议 | `MacClient/Sources/CCAnywhere/Services/ProcessHost.swift:149-151` | 找不到 claude 时返回 `/usr/bin/env` + args `["-c"]` → `env -c` 不会启动 claude，UI 无任何错误提示（前轮 #25 / 复审 #10 仍未修复） | 找不到时 throw / 在 SwiftTerm feed 红色错误条 |
| 20 | 🟢 建议 | `MacClient/Sources/CCAnywhere/Services/JSONLWatcher.swift:226-243` | 解析失败行仍混在 `msg.stream` 数组里（type="raw"），未独立 `msg.raw` envelope（前轮 #13 / 复审 #4 仍未修复）。Android 端 `msg.raw` 渲染分支永不命中 | 改用 `ws.send("msg.raw", {tab_id, line, parse_error})` |
| 21 | 🟢 建议 | `MacClient/Sources/CCAnywhere/Services/InputInjector.swift:67-71` | `image.fetched` 在 PTY 写入之前 `await ws.send(...)`；WS 阻塞会拖慢图片注入 PTY（前轮 #16 / 复审 #5 仍未修复） | 把 `ws.send(...)` 放到 `Task.detached`，PTY 注入立即执行 |
| 22 | 🟢 建议 | `AndroidClient/lib/data/ws_client.dart:169-170` | `_sendBind` 后立即 `_setState(connected)`，未等 `bind.ack`。INVALID_TOKEN 反例的窗口期内业务 send 会通过状态门禁（前轮 #11 / 复审 #7 仍未修复） | 移动 `_setState(connected)` 到收到 bind.ack 之后；或维护单独 authed 标志 |
| 23 | 🟢 建议 | `MacClient/Sources/CCAnywhere/Models/ProtocolMessage.swift:8-18` + `AndroidClient/lib/models/protocol_message.dart:5-20` | 三端 envelope 仍不含 `ts`（前轮 #18/#19 / 复审 #3 仍未修复）。Server 已下发，Mac/Android 既不消费也不发送 | 增加 `ts: String?` 字段；客户端发送时填 ISO8601 |
| 24 | 🟢 建议 | `AndroidClient/lib/data/image_upload_service.dart:126-150` | `awaitResponse(forTypes: imageUploadUrl,...)` 不带 begin id matcher。当用户连发 5 张图同时上传时，匹配 first 命中即返回——若 server 顺序乱（理论上不会但 hub 写 channel 有 64 缓冲）则 upload_url 错配 | `_attemptOnce` 让 awaitResponse 增加 matcher（基于响应 data 内 tab_id+filename，或自定义 correlation id） |
| 25 | 🟢 建议 | `MacClient/Sources/CCAnywhere/Services/InputInjector.swift:42 + 54` | `UUID(uuidString: p.tabId)` 校验失败时 `return` 静默。手机端发了无效 tabId（如已被关闭、id 拼错）UI 永远不知道 | tabId 校验失败时调 `reportInjectError` 反向通知（与 R-M7-05 一致） |
| 26 | 🟢 建议 | `AndroidClient/lib/models/server_config.dart:48-54` | `(j['port'] as num).toInt()` 未校验范围 [1,65535]；负数或 0 会让连接立即失败但无明确错误码 | parse 时显式校验，否则返回 `null` 让上游显示"配置无效" |
| 27 | 🟢 建议 | `Server/cmd/cc-anywhere/cli.go:38-66` | `reset-master-token` 重置后没踢当前 mac_connection（前轮 S7 场景第 5 步明确要求） | CLI 通过 IPC / socket 通知主进程；或文档明示需重启 server |
| 28 | 🟢 建议 | `MacClient/Sources/CCAnywhere/Services/PIDTracker.swift:60-81` | `isClaudeProcess(pid:)` 通过 `ps -p <pid> -o comm=` 判断；若 pid 已被复用为别的进程（macOS pid 回卷），可能误杀。概率低但应记录 | 增加二次校验（如 ppid != 1 / executable path 包含 claude） |
| 29 | 🟢 建议 | `Server/internal/server/server.go:299-360` `dispatchFromMac` | unknown 消息走 default → 调 `router.RouteFromMac` → 静默忽略。调试时不易发现协议契约错位（前轮 #26 仍未修复） | 增加 `slog.Debug("unhandled from mac", "type", env.Type)` |
| 30 | 🟢 建议 | `Server/internal/image/image.go:204-206` | Mac 离线时 `input.image` 通知静默丢失；inbox 文件依靠 5min GC，期间 phone 端无任何反馈直到 image.upload.expired | Mac 离线时立刻把 `image.upload.expired` 或 `error{MAC_OFFLINE}` 推给 phone |

---

## 审查结论

- [x] **通过：未发现阻塞问题**
- [ ] 不通过

### 总结

1. **本轮无新增 🔴 阻塞问题**。前两轮累计 11 个阻塞缺陷在复审中已确认修复。
2. **17 个 🟡 警告**：13 个本轮新发现（边界值、并发竞态、资源管理类），4 个为前轮遗留未触及（force_disconnect reason 区分、agent_id、msg.raw envelope、image.fetched 阻塞 PTY）。
3. **13 个 🟢 建议**：包含前轮全部 🟢 遗留 + 本轮新建议。
4. **三端协议契约**总体一致；envelope 容错与字段类型已统一处理。需求规格说明书 R-* 业务规则覆盖率高。
5. **数据库 schema** 符合需求规格说明书 §3.5.2 数据模型；WAL、busy_timeout、唯一约束、CHECK 约束齐全；只剩 migration 未事务化和过期 revoked 行累积两个 🟡。
6. **资源生命周期** 在正常路径完整；启动单次清理（inbox / 日志 / 旧 PID）的设计在长期运行时会失效。
7. **权限控制** 完整：HMAC 短期 URL、TLS 强制、bind 60s 强校验、单 Mac 单 token 踢旧。

**第二轮代码审查（边界值 + 业务影响 + 协议兼容 + 资源管理）通过，可进入第三轮上线研判 / 安全审查。**

建议优先在第三轮前修复 🟡 警告 #1（Android settings 短 sub_token 崩溃）和 🟡 警告 #2（4 MiB readLimit 切断大 JSONL 行），这两个是本轮新发现中实际触发概率最高的体验缺陷。
